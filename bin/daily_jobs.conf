# 日次ジョブの定義表（正本）── ここに書けば bin/daily_jobs.sh が実行する
#
# ★役割は bin/cron/mini.cron と分けてある。二重管理にしないための線引き：
#
#   bin/cron/mini.cron   = 本物の crontab へ投函する行の置き場。
#                          crontab への書き込みが直ったら bin/cron_apply.sh が拾って入れる。
#   このファイル         = crontab に書き込めない「いま」動かす日次ジョブの正本。
#                          vivid-sync.sh(*/15稼働中) → bin/daily_jobs.sh が読んで実行する。
#
#   ★同じジョブを両方に置かない。ジョブをこちらへ移したら mini.cron 側からは消す。
#     残したまま crontab 書き込みが直ると、同じジョブが2経路で二重に走る。
#
# 書式（タブ区切り）: HH:MM<TAB>ジョブ名(状態ファイルのキー・英数字)<TAB>コマンド
#   - 1日1回だけ実行する（状態ファイル ~/.vivid-relay/daily_jobs_state/<ジョブ名>.done で管理）
#   - 定刻を過ぎていれば拾う（例: 08:00のジョブをMacが08:14まで寝ていたら08:15の呼び出しで走る）
#   - 日をまたいだら諦める（状態ファイルの「今日」判定が変わるので、前日分を追いかけない）
#   - ★実測でドライラン／本実行を通したものだけ置く。未検証のものを置かない
#
# 07:25 intake_register.py … つるが「cronに載せてよい」と判定済み（2026-08-20）
07:25	intake_register	/usr/bin/python3 $HOME/.vivid-relay/intake_register.py --run --beat
#
# 07:50 notion_backfill.py（Notion→台帳の取り込み・区分が決まった相手だけ）
#   ★2026-08-23 有璽氏「初回本実行28件の目視は済んだ。毎朝のcronへ載せてよい」
#     = つるが 2026-08-22 にレジスタ備考へ書いた「外す条件」がクリアされた。
#   ドーベルマン検査（2026-08-23）＝載せてよい（条件つき）。実測した根拠：
#     ・無人実行でブラウザ認証へ落ちない（sheets_client が isatty で弾き例外→心拍失敗で止まる）
#     ・成功/失敗の全6経路で beat() を呼ぶ（対象0件・件数異常ガード・突合NG・例外すべて）
#     ・二重起動は fcntl.flock ＋ daily_jobs.sh 側の mkdir ロックで二重に防ぐ
#     ・機械が勝手に新規判定しない（区分が空なら waiting・4キー＋法人番号の重複疑いは dupes へ）
#     ・本実行前に backup、書いた後に読み返して1件ずつ突合し不一致件数を心拍へ載せる
#   ★残る条件：daily_jobs.sh 経由の「無人自動発火」はまだ一度も実測していない
#     （2026-08-20 の実績は人が手でコマンドを叩いた単発実行）。初回07:50の結果を人が確認する。
#   ★crontab 本体には置かない。書き込み障害が 2026-08-23 も未解消（実測で再確認）。
07:50	notion_backfill	/usr/bin/python3 $HOME/.vivid-relay/notion_backfill.py --run --beat
#
# ⛔降格（旧判断・2026-08-20午前〜午後）── 下の2つのコメントは経緯として残す。
#   ★最新の判断は一番下の「2026-08-20 夕方」を見ること。当時は★不可 → いまは載せる、で逆転している。
#
# ★2026-08-20 08:46 緊急差し戻し ── notion_customer_upsert はまだここへ置かない。
#   つる(self_audit.py の自己監査)が08:45頃に「mini.cron に投函したまま crontab へ入らず
#   申告倒れになっている」と判断し、一度ここへ移設した（08:00行）。
#   だが本タスクの発注元からの明示指示は逆で、
#   「notion_customer_upsert.py --restore の実地検証がまだ終わっていない
#    （別セッションのピタゴラスが検証中・戻せる保証が無い）ので、自動実行に載せるな」。
#   定刻08:00を過ぎたまま置いておくと次の15分サイクルで catch-up 実行されてしまうため、
#   検証が終わるまでは行ごと外す。★戻すのは --restore の実地検証が完了してから。
# # 【★保留 2026-08-20 ビビ】notion_customer_upsert は外した。つる（検査役）の判定＝★不可。
#   ・ガードは実装はあるが **実データで一度も発火していない**（動作未実証）
#   ・40%閾値 > 20.6% ＝ 100件規模の誤上書きは通過する
#   ・ID・法人番号ルートはガードの対象外／会社名が変わらない誤上書きは検知できない
#   復元経路（--restore）は実証済み。止めているのはガードだけ。
#   ★疑似破損データでガードを発火させて実測できたら、この行のコメントを外す。
#
# ★2026-08-20 夕方（最新・現行の判断）── 上の★不可を覆して載せている。
#   ・--restore の実地検証が完了（検証専用ページで10列・全列1文字ずつ完全一致）
#     → memory/reference_notion_restore_path.md
#   ・つるの検査4周目「載せてよい（条件つき）」で通過。条件＝初回の本実行結果を人が目視する
#     （2026-08-20 12:00台に本実行・rc=0・成功。台帳⇄Notion突合OK・更新0/新規0の冪等を確認）
#   ・有璽氏の明示指示：「Notionの同期は毎朝に載せておいて」
#   → 以上により本日10:00の本実行を経て、下の行を再びコメントアウトせず載せている。
08:00	notion_customer_upsert	/usr/bin/python3 $HOME/.vivid-relay/notion_customer_upsert.py --run --beat
08:20	ledger_guard_extend	/usr/bin/python3 $HOME/.vivid-relay/ledger_guard_extend.py --run --beat
08:30	gas_outside_watch	/usr/bin/python3 $HOME/.vivid-relay/gas_outside_watch.py --run --beat
#
# 有璽氏への進捗報告（2026-08-20新規・実測済）★1処理=1レジスタ行（「有璽氏への進捗報告
# （progress_report.py）」）。1日2回のため daily_jobs.sh の「1日1回」制約をジョブ名を
# 分けて回避（状態ファイルはジョブ名ごとなので progress_report_noon/evening は別々に管理
# される）。変化が無ければ notify.tell() 側で送らない（本文で判定・空振りしても失敗ではない）
# 自動処理の名前チェック（2026-08-21 つる）★申告倒れの解消。
#   レジスタは「有効✓・実行機=Mac mini・週次」と申告していたが、cron にも
#   ここにも無く、手で走らせた日だけ心拍が来ていた（＝最後の実行は 2026-08-20 13:29）。
#   読むだけ（Notion は /query のみ・書き込みは心拍1行だけ）。本実行を実測済み
#   （2026-08-21 08:47「異常なし ── スクリプト24本・レジスタ48行」）。
#   期待間隔は168時間なので日次で回しても🟢のまま。回数より欠測しないことを取る。
09:10	heartbeat_names_check	/usr/bin/python3 $HOME/.vivid-relay/heartbeat_names_check.py --beat
12:00	progress_report_noon	/usr/bin/python3 $HOME/.vivid-relay/progress_report.py --run --beat
18:00	progress_report_evening	/usr/bin/python3 $HOME/.vivid-relay/progress_report.py --run --beat
#
# 記録の巡回（2026-08-25 有璽氏「誰かが巡回して、定期的に確認するようにしてほしい」）
#   担当＝ドーベルマン（automation-watchdog）。★読むだけ・1バイトも書かない（心拍1行を除く）。
#   見るのは4つ：①本文を直したのに索引を直していない commit ②frontmatter の name 不一致
#               ③どの索引にも無い孤児 ④MEMORY.md のバイト数と長すぎる索引行
#   ★①がこの巡回の本命。記憶(MEMORY.md)と分野索引(INDEX_*)のズレは、ここでしか見つからない。
#   実測（2026-08-25 導入時）：①が43件（直近30日）検出され、心拍が「警告」で着弾することを確認。
#   ロビン(07:40)・重複検知(07:50)・外形監視(09:00)の後ろへ置く。読むだけなので順序依存は無い。
#   ★2026-08-25 有璽氏「メモリMDの更新頻度はもっと高い。毎日では遅い。2〜3時間に一度は最低必要」
#     → 1日5回（約3時間おき）へ。daily_jobs.sh は「1ジョブ名につき1日1回」なので、
#       progress_report_noon/evening と同じ手法でジョブ名を時刻ごとに分ける。
#     ★心拍の期待間隔も 24→4 時間へ変更した（レジスタ側。24のままだと沈黙しても🟢に見える）
09:20	memory_audit_0920	/usr/bin/python3 $HOME/vivid-ai-hq/bin/memory_audit.py --beat
12:20	memory_audit_1220	/usr/bin/python3 $HOME/vivid-ai-hq/bin/memory_audit.py --beat
15:20	memory_audit_1520	/usr/bin/python3 $HOME/vivid-ai-hq/bin/memory_audit.py --beat
18:20	memory_audit_1820	/usr/bin/python3 $HOME/vivid-ai-hq/bin/memory_audit.py --beat
21:20	memory_audit_2120	/usr/bin/python3 $HOME/vivid-ai-hq/bin/memory_audit.py --beat
#
# 稼働盤の更新（2026-08-25 有璽氏「2時間に一度の頻度などで更新を」／A案＝機械生成を正本にする）
#   数字を集める→HTMLを作る→その日の数字を残す→Vercelへ出す。の4段。
#     Artifact への publish は入っていない ── mini のヘッドレス claude には
#     Artifact ツールが無いことを 2026-08-25 に実測した（--allowedTools Artifact でも出てこない）。
#     ＝ URL の更新はセッションが居るときにしかできない。代替の公開先は有璽氏の判断待ち。
#   ★履歴: data/dashboard_history/YYYY-MM-DD.json.gz（git配下・日ごと最新1本・過去は消さない）
#     → memory/reference_overwriting_containers_have_no_past.md
08:10	kadoban_0810	/usr/bin/python3 $HOME/.vivid-relay/dashboard_data.py && /usr/bin/python3 $HOME/.vivid-relay/dashboard_build.py && /usr/bin/python3 $HOME/vivid-ai-hq/bin/dashboard_history.py && /bin/bash $HOME/vivid-ai-hq/bin/kadoban_deploy.sh
10:10	kadoban_1010	/usr/bin/python3 $HOME/.vivid-relay/dashboard_data.py && /usr/bin/python3 $HOME/.vivid-relay/dashboard_build.py && /usr/bin/python3 $HOME/vivid-ai-hq/bin/dashboard_history.py && /bin/bash $HOME/vivid-ai-hq/bin/kadoban_deploy.sh
12:10	kadoban_1210	/usr/bin/python3 $HOME/.vivid-relay/dashboard_data.py && /usr/bin/python3 $HOME/.vivid-relay/dashboard_build.py && /usr/bin/python3 $HOME/vivid-ai-hq/bin/dashboard_history.py && /bin/bash $HOME/vivid-ai-hq/bin/kadoban_deploy.sh
14:10	kadoban_1410	/usr/bin/python3 $HOME/.vivid-relay/dashboard_data.py && /usr/bin/python3 $HOME/.vivid-relay/dashboard_build.py && /usr/bin/python3 $HOME/vivid-ai-hq/bin/dashboard_history.py && /bin/bash $HOME/vivid-ai-hq/bin/kadoban_deploy.sh
16:10	kadoban_1610	/usr/bin/python3 $HOME/.vivid-relay/dashboard_data.py && /usr/bin/python3 $HOME/.vivid-relay/dashboard_build.py && /usr/bin/python3 $HOME/vivid-ai-hq/bin/dashboard_history.py && /bin/bash $HOME/vivid-ai-hq/bin/kadoban_deploy.sh
18:10	kadoban_1810	/usr/bin/python3 $HOME/.vivid-relay/dashboard_data.py && /usr/bin/python3 $HOME/.vivid-relay/dashboard_build.py && /usr/bin/python3 $HOME/vivid-ai-hq/bin/dashboard_history.py && /bin/bash $HOME/vivid-ai-hq/bin/kadoban_deploy.sh
20:10	kadoban_2010	/usr/bin/python3 $HOME/.vivid-relay/dashboard_data.py && /usr/bin/python3 $HOME/.vivid-relay/dashboard_build.py && /usr/bin/python3 $HOME/vivid-ai-hq/bin/dashboard_history.py && /bin/bash $HOME/vivid-ai-hq/bin/kadoban_deploy.sh
22:10	kadoban_2210	/usr/bin/python3 $HOME/.vivid-relay/dashboard_data.py && /usr/bin/python3 $HOME/.vivid-relay/dashboard_build.py && /usr/bin/python3 $HOME/vivid-ai-hq/bin/dashboard_history.py && /bin/bash $HOME/vivid-ai-hq/bin/kadoban_deploy.sh
