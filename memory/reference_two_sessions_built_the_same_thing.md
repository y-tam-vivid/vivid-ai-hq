---
name: reference_two_sessions_built_the_same_thing
description: 別マシンのセッションへは SendMessage が届かない。二重実装を救うのは冪等性。着手宣言が最後の防波堤
metadata:
  node_type: memory
  type: reference
---

**別のマシンで動いているセッションとは、直接やり取りできない。** `ListAgents` に出るのは
**同じマシンのセッションだけ**で、`SendMessage` もそこへしか届かない。
相手が動いていることは `ssh <機械> 'ls /tmp/cc-socks/'` で分かるが、**話しかける手段は無い。**

**Why:** 2026-08-20、MacBook と Mac mini が **同じ「00_企業マスタ → Notion🏢顧客DB の upsert」を
別々に実装した。** どちらも着手宣言を先に書いていなかった。

```
00:09  MacBook  upsert を実行  382件 → 486件（補完309・新規104）
02:42  mini     notion_customer_upsert.py を本実行  → 更新0・新規0
                （＝MacBookが先に書き終えていたので、書くものが無かった）
```

**実害はゼロだった。冪等に作ってあったから助かっただけで、設計で防いだのではない。**
片方が「全部消してから作り直す」型だったら、もう片方の結果は消えていた。

**How to apply:**

```
連携の経路（上から順に試す。★1つで足りると思わない）
  ① WORKING.md へ着手宣言 → commit      正規の経路。ただし相手が pull 停止中だと届かない
  ② 相手のローカルへ直接ファイルを置く    確実。~/.vivid-relay/HANDOFF_*.md など
  ③ Notion（⑥／③）                     両者が見る場所。非同期でよいならここ
  ✕ SendMessage                        ★別マシンには届かない
```

- **相手が `git pull` 停止中なら ①も届かない。** 未コミットがあると ff-only が止まるため。
  こういうときは②を使う。**「commit したから伝わったはず」は成立しない。**
- **書くものは冪等に作る。** 2回走っても壊れないなら、二重実装は事故にならず「無駄」で済む。
  `update` は空欄補完のみ、`create` は突合してから、`clear`→`rebuild` は避ける。
- **着手宣言は「相手のため」ではなく「自分が後で誤解しないため」でもある。**
  2026-08-20 は両セッションとも宣言なしで走り、あとから WORKING.md を見て初めて
  同じものを作っていたと分かった。
- 機械側の補強 → `bin/vivid-sync.sh` が未コミットのファイル名を `SYNC_STATUS.md` へ書き出す
  （宣言を忘れても、触っている実態は見える）。関連 → [[reference_silent_sync_failure]]

---

## ★別の面で作られたファイルは、このマシンには存在しない（2026-08-28 実地）

claude.ai のチャットが `event-skills-for-claude-code.tar.gz` を「アーカイブにしました」と告げ、
有璽氏がその展開コマンドをこちらへ貼った。**そのファイルは MacBook にも Mac mini にも無かった。**
Downloads・Desktop・/tmp・Drive のどこにも無く、**チャットの添付としてしか存在しなかった。**

```
チャットが「作った」        添付リンクとして存在する（そのチャットの中だけ）
このマシンに「在る」        ~/Downloads へ人が保存して初めて成立する
                           ★この2つの間に自動の橋は無い
```

- **★「作った」「アーカイブにした」は受け渡しの完了ではない。** 面が違えば、
  作った側は自分のマシンへ置けない。**人が1回ダウンロードするまで、実体はゼロ。**
- **★コマンドを貼られても、その前提（ファイルの実在）を先に確かめる。** 2026-08-28 は
  `tar xzf` を打つ前に `ls` で確かめたので、`cp -r` が既存スキルへ何かする前に止まった。
  **貼られた手順は「実在の証拠」ではない。**
- **★受け渡しの正解は git。** ファイルを人が置く → こちらが `vivid-ai-hq` へ commit →
  もう一方の機は `bin/vivid-sync.sh`（15分ごと）で自動的に受け取る。
  **手で mini へ scp すると、以降の改訂のたびに二重管理になる**（有璽氏の指摘・2026-08-28）。
  → [[reference_fix_where_git_reaches]]
- **★base64 で Drive へ流すのは採らない。** バイナリは1バイト崩れると展開できず、
  失敗が「壊れたファイル」として静かに残る。テキストへばらすのも写経＝転記事故を増やす。
  **人が1回ダウンロードするのがいちばん確実で速い。**
## ★同名の untracked ファイルが、同期を永久に止める（2026-08-29 実測）

**2つのセッションが同じファイルを作ると、git の同期がその瞬間から止まる。**
しかも `vivid-sync.sh` の自動処理では解けない。

```
起きたこと
  MacBook 側が bin/setup_ffmpeg.sh を作って push
  mini 側でも同じものが作られた（★untracked のまま・commit していない）
  → merge しようとすると
     error: The following untracked working tree files would be overwritten by merge:
            bin/setup_ffmpeg.sh
  → ★未取込 9件のまま固定。SYNC_STATUS.md が🔴になり
     「いま読んでいる WORKING.md / MEMORY.md は古い」状態が続く
```

**★`vivid-sync.sh` は枝分かれ（behind>0 かつ ahead>0）には対処済み**（過去に
behind=11/ahead=1 で孤立した事故を踏んで `git merge --no-edit` を入れてある）。
**だが untracked の同名衝突は、その merge も弾く。** 自動では絶対に解けない。

### 直し方（可逆・消さない）

```
1  退避する（消さない・移動のみ）   mv して scratchpad へ
2  git merge --no-edit origin/main
3  ★退避した版とリモート版を diff で比較する
   2026-08-29 の実測では **完全に同一**だった（＝両セッションが同じものを作っていた）
4  違っていたら、退避した版の中身を確認してから判断する
```

### ここで一度、誤った断定をしかけた

担当が「`vivid-sync.sh` の自動処理に委ねます」と報告したのに対し、こちらは
**「それは成立しません」と断定しかけた**。実物（`bin/vivid-sync.sh` 90-105行目）を読んだら
枝分かれは既に扱えていた。**担当の判断のほうが正しかった。**
本当の原因は別（untracked の同名衝突）で、**それは読むまで分からなかった**。
→ [[feedback_one_route_is_not_verification]]

**★🔴 が出ている間に現在地を答えない。** 今日、🔴 が出ている状態で有璽氏へ報告を続けていた。
規範（fukuchi-core「届いたものがいつの版か」）に「先に取り込む」と書いてある。


## ★発行と受信が別の機械だと、押しても「見つかりません」になる（2026-09-03）

プッシュ型の判断経路（Slackのボタン）を作ったとき、**運用の決まりが1つ増えた。**

```
★発行は必ず mini から
理由  受付番号の台帳（ask_hub_queue.json）は★出した機械のディスクにできる
      MacBook から出すと、受信側（mini の slack_socket.py）から台帳が見えない
      → ★押しても「受付番号が見つかりません」になる
```

**★「両機に同じ道具を入れる」だけでは足りない。** 道具は両方に在っても、
**状態（台帳）が片方にしかできない**なら、**動く機械は1つに決まる。**
```
道具（コード）   両機に入れてよい。★入れた
状態（台帳）     ★片方にしかできない → ★発行する機械を1つに決める
受信（常駐）     mini にしか無い（MacBook に SLACK_APP_TOKEN が無く原理的に動かない）
```

**★この決まりは「気をつける」では守れない。** MacBookから出した回に必ず壊れる。
**機械で止める仕組みが要る**（発行時に受信側の常駐と同じ機械かを確かめて、違えば止める）。
→ [[reference_hooks_enforce_what_discipline_cannot]]
★いまは決まりだけ。**止める仕組みは未実装**と、そう書いておく。

## ★リンクは「押した先に何があるか」を確かめてから付ける（同日・良い形）

承認した文面に「▶ 現物を開く」というリンクを入れていた。担当が**自分で外した。**
> 「押しても**同じ画面に戻るだけで意味が無い**ので外しました」

**★リンクを付けると、読む側は「押せば何か分かる」と期待する。**
期待に応えない導線は、**無いより悪い。**（押して戻ってきた分、時間を使わせている）
→ [[feedback_write_for_the_reader]]

## ★同じ機械でも二重になる ── DM 1通が2つのビビを起こす（2026-09-04・実害あり）

**別マシンの話だと思っていた。同じ機械の中でも起きる。**

```
有璽氏がSlack DMへ1通書く
   ├─▶ ターミナルで動いているビビ（このセッション）が受ける
   └─▶ slack_inbox.py（cron */5）が拾い、claude -p でビビを**もう1つ起動**する
        実測：ps に PID 15936 `claude -p 「あなたは「ビビ」…」`＋同じDM本文が丸ごと
        08:55 起動・09:17 時点で22分以上走行中
```

**実害（2026-09-04）** ── 2つのビビが同じ `~/.vivid-relay/notify.py` を触った。

```
09:03  こちら側のピタゴラスが改修（ask()をask_hub委譲＝ボタン化）
09:15  ★notify.py が改修前と完全一致する内容へ巻き戻る（こちらの操作ではない）
       実測2経路 ＝ ①sha256 が _backups/notify.py.bak_…0901 と完全一致
                    ②grep で ask_hub 0件・「番号だけでOK」の旧文言が復活
       ＝ 有璽氏がその朝に指摘した記述式が、そのまま戻った
```

- **★着手宣言（WORKING.md）は、相手が読まない経路で起動されていると効かない。**
  `claude -p` で起動された側は WORKING.md を読むが、**こちらが書く前に走り出している。**
- **★同じ依頼を2つの窓口で受けない。** 受け口が2つある構造そのものが原因。
  直すならどちらかに寄せる（例：ターミナルで対応中は slack_inbox の起動を抑止する）。
  **これは設計の分岐＝有璽氏の判断が要る。勝手に片方を止めない。**
- **★巻き戻りは「壊れた」より見つけにくい。** 動くし、エラーも出ない。
  **版を固定して sha256 を取る**運用だけが検知できた → [[reference_freeze_the_version_under_review]]。
- **★検査役が「対象が消えた」と言ってきたら、まず自分で数え直す。**
  今回はステラの申告どおりだった（こちらも独立に2経路で確認して一致）。

## ★1日で5回起きた。MacBook側だけ。miniは無事（2026-09-04 実測）

同じ `notify.py` が **09:15 ／ 10:45頃 ／ 10:53頃 ／（作業終了後にもう1回）** と巻き戻った。

```
実測（ピタゴラスの作業直後にビビが独立に確認）
  MacBook  grep -c ask_hub = ★0      sha256 5c3de558…
  mini     grep -c ask_hub = 27      sha256 d0a23d0d…
→ ★不一致。ピタゴラスの「両機で一致をsha256で確認済み」は、その後にまた戻った
  稼働中の claude プロセス ★20個
```

- **★実害は今のところ無い。定期実行はすべて mini が持つ**（fukuchi-core「定期実行は片方だけ」）。
  MacBook 側の `~/.vivid-relay/` は**誰も実行していない写し**。ここが戻っても Slack の挙動は変わらない。
  **だが「両機を同じ環境にする」原則は破れている。** 片方を正、片方を写しと呼び分けるまで直らない。
- **★原因は特定できていない。** claude プロセスが20個走っており、どれが書いたか追えなかった。
  **着手宣言（WORKING.md）にも該当の宣言は無かった** ＝ 宣言に頼る仕組みが効いていない。
- **★対処は「検知」であって「復元」ではない。** `self_audit.py` に
  `_notify_ask_hub_check()` を新設し、notify.py を実読して委譲の実在を見る。
  無ければ「★人が要る」へ回す。**機械が勝手に書き戻さない**
  （書き戻す側が増えると、巻き戻りと区別がつかなくなる）。
- **★「両機で一致を確認した」は、確認した瞬間の事実でしかない。**
  巻き戻る環境では、**報告を読んだ側がもう一度数える**しかない。

## ★`git add -A` は他人の作業中の変更を巻き込む（2026-09-04・軽微）

ビビが memory を書いてコミットしようとしたら **「nothing to commit」**。
担当が着手宣言を書いた際の `git add -A` が、**ビビの未コミットの追記ごと拾っていた。**

```
実測  grep で追記は実在（内容は無事）
      git log -1 -- <そのファイル> → ★別人のコミット e37d546 に含まれていた
```
- **実害は無い**（消えたのではなく、別のコミットに入っただけ）。
  ただし**履歴を追う人は、そのコミットメッセージからこの変更を見つけられない。**
- **★同じリポジトリを複数が同時に触る前提では、`git add -A` は安全ではない。**
  自分が触ったファイルを名指しで add する方が正確。
  ★ただし全員に徹底させるのは規律依存なので、**「巻き込まれることがある」と知っておく**方を採る。
- **★「nothing to commit」を見たら、消えたと思う前に grep で中身を確かめる。**

## ⛔2026-09-05 訂正 ── 「miniは無事」は誤り。実行機も巻き戻っていた（つる実測）

上の「MacBook側だけ・miniは無事＝実行機は無傷」は、**mini の実物では成立していない。**

```
実測（mini ~/.vivid-relay/・2026-09-05 08:4x）
  notify.py       124行 ／ ask_hub 0件      ← 改修前の版
  self_audit.py   ask_hub / _notify_ask_hub_check 0件
  memory_sweep.py ask_hub 0件
  slack_inbox.py  answered ガード 5件       ← ★これだけ残っている
  控え2本（_backups/notify.py.bak_*-0901 と *-1_mini）も 124行・ask_hub 0件
```

- **★改修版はどこにも残っていない。** 実物にも、指定された控えにも、1バイトも無い。
  「mini から scp で復元した」という 9/4 の申告は、**いま実物で確かめると成立していない。**
- **★最初に消えるのは検知役。** `self_audit._notify_ask_hub_check()`（巻き戻りを見つける役）が
  巻き戻りで消えた。**検知を同じ場所に置くと、検知ごと消える。**
- **実害（9/5 08:30 実測）**：`findings_escalate` が `notify.ask()` を呼び、
  `slack_pending.json` へ**記述式**（番号で返す形）で「営業台帳の指摘・8日連続」を書いた。
  `ask_hub.log` の最終は 9/4 17:49 ＝ **ボタン経路には今日1件も出ていない。**
  有璽氏が2回名指しで禁じた形が、いま自動で出続けている → [[project_ask_hub_push_decisions]]
- **★「両機でsha256一致を確認した」を根拠にしない。** 一致は確認した瞬間の事実。
  巻き戻る環境では、**次に読む人がもう一度数える**。数えずに引き継ぐと、
  今回のように「無事」という誤った前提が索引を通って1日流通する
  → [[reference_stale_premise_daily]]

---

## ⛔🔴 訂正 ── 「ビビが2体走って巻き戻した」は誤診だった（2026-09-06 実測・ピタゴラス）

**2026-09-04 に `notify.py` の改修が5回消えた件を、このファイルは「同じ依頼が2つのセッションへ
入り、同じファイルを触ったから」と説明していた。★違った。**

```
真因   bin/vivid-sync.sh:194 が cron */15 で bin/setup_hooks.sh を★無条件に呼ぶ
       bin/setup_hooks.sh:38-47 が bin/hooks/*.py を ~/.vivid-relay/ へ★上書きコピーする
       ＝ ~/.vivid-relay/ だけを直すと、15分以内に bin/hooks/ の旧版で必ず消える
```

**実測（2経路）** ── 経路1＝`notify.py` は5コピーとも同じ sha・`ask_hub` 0件
（実物2つ＋控え3本。9/4版も9/5版もどこにも残っていなかった）／
経路2＝層3が新設した `self_audit._notify_ask_hub_check()` が「★巻き戻り疑い」を返した。

```
消えた   ~/.vivid-relay/notify.py だけを直した   → 15分後に bin/hooks/ の旧版で上書き
残った   bin/hooks/self_audit.py を直した        → git配下＝★配る側なので消えない
★差は「git 管理下に書いたかどうか」の1点だけ。セッションの数とは関係が無い
```

- **★同じ日に、同じ人が、片方は消えて片方は残った。** これが誤診を生んだ。
  「2体走っていた」という観測は事実だが、**因果ではなかった。**
  ★**同時に起きた事象を原因にしない。**片方だけ残った理由を説明できるまで、真因と呼ばない。
- **★配布の向きを確かめてから直す。** `~/.vivid-relay/` は**受け取る側**。
  ここを直すのは「配られたコピーを直す」ことで、**次の配布で必ず消える。**
  → [[feedback_read_the_artifact_not_the_copy]]（写しでなく実物を読む）の書き込み版。
- **★「巻き戻った」を見たら、まず上書きする側を探す。** 犯人はセッションではなく cron だった。
- ★同じ型は `~/.vivid-relay/` 配下の全ファイルに効く。**git 管理外だから安全、ではない。**
## ⛔⛔2026-09-05 夜 再訂正 ── 真因は「2セッション」ではなかった（ピタゴラス実測）

**上の 9/4「ビビが2体走っていた」という説明は、notify.py の巻き戻りの原因としては誤り。**

```
実測（2026-09-05 18時台・2経路）
  経路1  bin/vivid-sync.sh:194 が */15 で bin/setup_hooks.sh を★無条件に呼ぶ
         setup_hooks.sh:38-47 が bin/hooks/*.py を ~/.vivid-relay/ へ★上書きコピーする
  経路2  同じ日に直した bin/hooks/self_audit.py は★生きている
         （_notify_ask_hub_check が実在し、走らせると正しく判定を返す）
  ＝ 消えたのは「~/.vivid-relay 側だけを直した」ファイルだけ。セッション数と無関係
```

- **★このファイルの型（2セッションが同じものを作る）自体は実在する。** 消さない。
  ただし **notify.py の巻き戻りはこの型ではない。** 犯人を取り違えたまま索引に載せると、
  正しい対処（正本を `bin/hooks/` 側にする）に永久に辿り着かない。
- **★「原因を1つ見つけた」で止めない。** 9/4 は ps で2プロセスを見つけた時点で確定させた。
  **同じ日に別のファイルが生き残っていた**という反証を数えていれば、その場で否定できた
  → [[feedback_one_route_is_not_verification]]
- 対処と見分け方は [[reference_fix_where_git_reaches]] の末尾に集約した（本文はそちらが正本）。

## ✅ 2026-09-06 ── 型が本物であることを実地で確認。**書く前に止められた**

**承認ダイアログの改修が、同一の依頼文で2セッションへ入っていた。**

```
実測  ①指示文が同一（~/.vivid-relay/_task_approval_dialog.txt ⇄ こちらの起動プロンプト）
      ②ps -axo etime  こちら PID 68242（19:22経過）／相手 PID 69505（14:41経過）
        ＝こちらが先。相手は4分41秒あとに起動
      ③相手は既に ~/.vivid-relay/approval_dialog_result.md を書いていた（09:00）
```

**★止められたのは、書き込みの前に実物を1回見たから。**

```
確認したもの  bin/hooks/hook_permission_slack.py の sha256 と git status
結果          sha256 は元のまま（5545168a…）・mtime 8/20・git差分0 ＝★まだ誰も書いていない
判断          止めるなら「いま」。★片方を止めても成果はファイルに残る＝可逆
              二重に書き込むと片方の改修が消える＝不可逆
              → 規範「迷ったら戻せるかで判断する」に従い、止められる方（自分の
                サブエージェント）を止めた。相手のプロセスは kill しない（不可逆）
```

- **★「先に宣言した方が続ける」は、成果の量では決めない。**
  こちらが先に WORKING.md へ宣言していたが、**相手は既に成果を書いていた。**
  規範どおり先着を通すより、**降りて実測と申し送りに徹する方が、全体としては速い。**
  ★窓口が束ねる前提なら、担当が張り合う理由は無い。
- **★発見は補完的になる。**こちらは「非対話では発火しない」「点検由来のニセ通知17件」、
  相手は「confined session では allow が無視される」「押した人を照合していない」を持っていた。
  **どちらか一方だけでは設計を誤る。**申し送りは**別名のファイル**に置く
  （相手の出口ファイルへ書くと、相手の上書きで消える）。
- **★二重に入ったこと自体は窓口の側の問題。**担当は気づいた時点で報告する。
  → [[feedback_use_the_team_not_alone]]（窓口はビビ一人）

## 🔴 3例目 ── 同じ目的の見張りが2本でき、片方が申告なしで本番稼働していた（2026-09-07）

```
2026-09-05  ビビ→ピタゴラス  層1 stall_watch.py（走行中の担当が無言で止まっていないか）
2026-09-06 08:51  ★別セッション  agent_watchdog.py（164行）── ★同じ目的
                  ★crontab */5 に実登録・発火実績あり
                  ★WORKING.md・memory のどこにも記載なし。着手宣言も無い
```

**★危ないのは「二重に作った」ことではなく、片方が誰にも見えないまま権限を持っていたこと。**

```
判定方法   CPU使用率 ＋ 対象ディレクトリの更新
           ★このCPU指標は、層1の作者自身が実測で「働いている0.033〜0.043秒/秒 対
             入力待ち0.019秒/秒＝2倍も違わないので判定に使えない」と結論した指標そのもの
危険機能   --kill-stuck で kill -9 する経路（既定オフ）
心拍       ★打っていない → 死んでも🔴が立たない
通知       notify.tell()（記述式）★有璽氏が4回求めている選択式ではない
```

**★なぜ見つからなかったか（実測）**

```
heartbeat_names_check.py（毎朝09:10）  ★対象外。beat() を呼ばないファイルは走査しない設計
                                       ＝「登録漏れを検出する仕組み」が、心拍を打たない
                                         スクリプトを★最初から見ていない
automation_inventory_check.py（週次月）  ★拾える。crontab の実行行を直接見るため
                                       ただし週次なので、最大7日間 誰も気づかない
```

- **★心拍を前提にした検査は、心拍を打たないものを構造的に見逃す。**
  「登録漏れ検査」が登録漏れを見逃す、という自己言及的な穴。
  → [[reference_heartbeat_proves_life_not_results]]／[[project_automation_register]]
- **★crontab を直接読む検査を、日次にする。**週次では遅い。
- **★新しく自動処理を作ったら、心拍と着手宣言はセット。**どちらか片方では見つからない。

### ★有璽氏の決定（2026-09-07）── 「見張りについて統合して動くような状態に持っていって」

```
統合先   stall_watch.py（★transcript の mtime で判定＝実測で正しいと確認済み）
止める   agent_watchdog.py ── ★消さない。.disabled で残す（規範「消さずに状態で表す」）
         ★取り込むもの：既にcronで回っていた実績／対象ディレクトリの更新を見る観点
条件     ①心拍を打つ ②通知は ask_hub のボタン（記述式にしない）
         ③★自動でkillしない。止めるのは人がSlackで押したときだけ
④        ★cronへ載せて「動く状態」にするところまでが依頼（＝載せずに終わらない）
```

- **★「統合」は片方を消すことではない。**判定の正しい方に寄せ、稼働の実績がある方から
  学べるものを取り込み、**残す方を1本にする。**
- ★2本が並んだ状態で放置すると、同じ止まりに2通の通知が出る＝
  [[project_ask_hub_push_decisions]]「押せないものが混ざって判断が沈む」を自分で作ることになる。

## 🔴 4例目 ── vivi_patrol.py（2026-09-08 つるの自己監査で発見）

**agent_watchdog.py を除去した翌日に、同じ型がもう1本立っていた。**

```
実物   ~/.vivid-relay/vivi_patrol.py（2026-09-07 20:51 新設）・crontab の */5 で本番稼働
欠け   ①WORKING.md に着手宣言なし ②memory に記述なし ③⚙️レジスタに行なし ④beat() 0件
実測   crontab に実在 ／ vivi-patrol.log に「異常なし（08:30/08:35/08:40）」が5分おきに実在
       ／ grep beat は0件 ／ automation_inventory_check.py が「★登録漏れ」として1件だけ検出
```

- **★「統合して1本にした」の翌日に2本目が生える。**止めたのは実体であって、
  **申告せずに cron へ載せる作り方そのものは止まっていない。**
- **★見つけたのは automation_inventory_check.py（crontab を直接読む方）だけ。**
  心拍を前提にした `heartbeat_names_check.py` は「異常なし ── スクリプト32本・レジスタ58行」を返した。
  3例目で書いたとおり、**心拍を打たないものは心拍前提の検査から構造的に見えない。**
  ★ただしこの検査は **週次（月曜09:20）**。今回つるが日次で回さなければ、最大7日 誰も気づかなかった。
  → **crontab を直接読む検査を日次にする**（3例目で書いた提案は、まだ入っていない）。
- つるが打った手＝レジスタへ行を新設（有効=False・既知=True）。**コードは書いていない。**
  beat() の追加と stall_watch.py との役割重複の判断は人／ピタゴラスへ。

### ★あわせて見つかった別の型 ── 実行頻度を変えて、監視の期待値を据え置いた

2026-09-07 に稼働盤を「2時間おき8回」→「crontab の */10」へ移した際、
**レジスタの期待間隔(時間)が 4 のまま残っていた。** 10分おきの処理を、
**6時間止まってから初めて🔴にする設定**で見張っていたことになる（有璽氏の依頼は「リアルタイム」）。
つるが 4 → 1 へ直した。**★頻度を変えたら、同じ手で期待間隔も変える。**
→ [[reference_heartbeat_proves_life_not_results]]

## 🔴 5例目 ── 二重になったのは**記憶ファイルそのもの**だった（2026-09-08／発見 09-09 ロビン）

これまでの4例はコード・見張り・受信経路だった。**今回は memory/ が二重化した。**
同じ有璽氏の1つの発言から、2台がそれぞれ別のファイルを作っていた。

```
発言（1つ）  2026-09-08「一口にRPOと言っても何をやるのかわからない。…中身がなくない？」
mini 側      memory/feedback_deliverable_granularity_must_be_actionable.md
origin 側    memory/feedback_break_down_to_the_work_level.md
実測         両ファイルとも同じ発言を引用している（本文を1行ずつ突合）
             MEMORY.md の索引にも、両方の行が別々に足されていた
             ＝ マージしたときに初めて衝突として現れた（それまで誰も気づけない）
```

### 真因は「枝分かれしていた19時間」── 遅れは受信だけでなく**送信も**止める

```
分岐点   54b9ad2  09-08 13:33   ここから両機が別々に進んだ
mini     8de82fe  09-08 15:45   deliverable_granularity を自動確定でcommit（分岐の後）
                                ＝ origin 側からは最後まで見えない
発見時   behind 40 ／ ahead 17  ★19時間、こちらの記録は1件も相手へ出ていない
```

- **★`bin/vivid-sync.sh` は `BEHIND=0` のときしか push しない**（コード実測）。
  つまり**遅れているマシンは、自分の記録を1つも他機へ出せない。**
  「遅れ」は受信の問題に見えて、**同時に送信の停止でもある。**
- **★自動マージは `DIRTY=0` のときしか走らない。`DIRTY` は `git status --porcelain` の行数＝
  untracked も数える。** 今回止めていたのは `data/dashboard_history/2026-09-09.json.gz`
  ＝ **自分たちの稼働盤 cron が毎日置いていく1ファイル。**
  自分で置いたゴミ1個で、自分の同期が止まる形になっていた。
- **★SYNC_STATUS.md の🔴は「未取込40件」としか言わない。**「あなたの記録17件が
  相手へ届いていない」とは書いていない。読む側は受信だけの問題だと読む。

### How to apply

```
◎ 記録する仕事（ロビンの毎朝の棚卸し・memoryへの追記）は、★先に取り込んでから始める
  cd ~/vivid-ai-hq && git status && git add -A && git commit && git merge origin/main
✕ 🔴が出たまま「昨日の学びを拾う」を始める ── 相手が既に書いたものを、もう1本書く
◎ 衝突したら両方を残す（規範どおり）。★ただし MEMORY.md は上限があるので
  両方の索引行は載らないことがある → 片方は分野索引へ降ろす
  → [[feedback_memory_index_hygiene]]
◎ 重複した2本は、その場で片方を消さない。★索引行に「重複・統合は人の判断」と書いて残す
```

**★いま残っている重複（未統合・人の判断待ち）**
`feedback_deliverable_granularity_must_be_actionable.md` ⇔ `feedback_break_down_to_the_work_level.md`。
前者の [[project_fukushi_needs_and_team_design]] は**存在しないファイルを指している**
（実体は `project_fukushi_pain_list_team_design.md`）。統合するならここも直す。

### ★2026-09-09 mini・同一cwdを2体が同時編集（gitの分岐とは別の型）

**これはマシン間の分岐ではない。同一マシン・同一 `~/lifestandup-wp/` の working tree を、
2つの `claude -p` プロセスが同時に編集していた。**

```
pid 6491（自分）  12:44起動  マスキングテープの定位置＋恒久ルール③④（WORKING.mdに着手宣言あり）
pid 6856（別）    12:45起動  9/9指摘15件の再修正（有璽氏差し戻し「赤枠=写真が動いていない」）
                            ★WORKING.mdに着手宣言なし。ps auxのコマンドラインでのみ発見
```

- **★発見の経路＝git working treeの中身**。コミット前の差分に「2026-09-09 3周目」という
  自分が書いていないコメントが複数CSSファイルに混入していた。これで気づいた。
  `git status`だけでは分からない（両方とも同じファイルをmodifiedにするため）。
- **★依頼の中身は競合していなかった**（写真の位置移動 vs テープの右端固定）が、
  **同じCSSファイルの同じ要素（.tape-vertical等）の近くを触る**ため、Editのold_string
  不一致という形で衝突が顕在化するリスクがあった。
- **対処**: Writeは使わずEditのみに限定（old_string不一致が衝突検知として働く）。
  相手の担当ページ・セレクタを先にps確認で洗い出し、被らない範囲から着手。
  相手の完了（プロセス終了）を待ってから、被る可能性のあった範囲を再実測。
- **★着手宣言の抜け漏れが再発している**。ビビ/有璽氏が新しいセッションを立てるとき、
  WORKING.mdへの1行を先に書く運用が徹底されていない。`ps aux | grep "claude -p"`で
  他プロセスの有無を確認するのは着手宣言が無い場合の最後の砦。

## 🔴2026-09-09 5例目 ── ★worktree で分けても、同じCSSの同じ箇所で競合した

**★「作業ツリーを分ければ並走できる」は半分しか正しくない。** 触るファイルが重なれば、
分けたのは編集の場所だけで、**★統合の手間はあとでまとめて来る。**

```
経緯  13:07 ビビが wt/tape（worktree・ブランチ wt/tape）へテープの定位置を投げた
      同じころ main 側でも「赤枠(写真)の移動」が走っており、★同じCSSを触っていた
      15:24 wt 側 完了（独自commit 9件）
実測  共通祖先 a74d2b8（11:07）
      祖先以降  main 3件 ／ wt 9件
      ★mergeのドライラン → ★7ファイルが競合（about-ilife/about-staff/recruit-interview/
        recruit-values/stand-up-top/testimonials/style.css）→ ★abortして main は無傷
```
**⛔ビビは一度「作業の中身が別だから重複ではない」と読んだが、誤りだった。**
★ファイル名の重なりだけでなく、**★merge のドライランまでやって初めて競合が分かる。**

**★投げる前に見るべきもの（順に）**
```
① いま main 側で何が走っているか（★WORKING.md だけでなく★走行中プロセスと直近commit）
② 投げようとしている作業が触る★ファイルの見当
③ ①と②が重なるなら、worktree で分けても意味は薄い ── ★順番にやる方が速い
```
**★競合が出たあとの判断（どちらの実装を採るか）は、担当でなく★窓口が出す。**
今回は担当が先にボタンを出した（#901d3d）。**★窓口が投げた作業の後始末は窓口が持つ。**

## 🔴2026-09-09 16:3x ★公開スクリプトが、走行中の担当のサーバを殺す（未然に止めた）

```
状況   merge の検証が全項目通り、★公開できる状態になった
       同時に pita_wp_run が★ローカル検証WordPress で134本を投入中
発見   `redeploy.sh:21` が ★`pkill -f "php -S 127.0.0.1:8750"` から始まる
       ＝ ★公開を打った瞬間、ローカルWPを使っている担当のサーバが落ちる
       （62行目でも終了時に pkill する）
判断   ★公開を待つ。★止まったのではなく、衝突を避けた
```
**★「触るファイルが重ならないか」だけでは足りない。★ポート・プロセスも重なる。**
```
★並列に投げる前に数えるもの（今日ここまでで3つに増えた）
  ① 触るファイル（★9/9午後に競合7本で学んだ）
  ② ★ポート／プロセス（★これ。8750 は redeploy も検証も同じものを使う）
  ③ 外部の枠（Vercelのデプロイ回数など・1日100回）
```
**★公開スクリプトは「後始末」として pkill を含むことがある。**
★他人が立てたサーバも巻き添えにする。**★実行の前に、そのスクリプトが何を kill するか読む。**

**★索引から本文へ戻した（2026-09-09・索引の長さを詰めたため）**
- vivi_patrolが申告なしでcron*/5稼働（心拍なし＝心拍前提の検査は構造的に見逃す・除去の翌日に生える）
- 🔴9/8 5例目＝★記憶ファイルが二重化（同じ発言から2本）。真因は19時間の枝分かれで記録が相手へ届かないこと。★棚卸し・記録は取り込んでから始める**
