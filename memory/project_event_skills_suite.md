---
name: project_event_skills_suite
description: 対外イベント運用の6スキル群（event-*）の構成と、有璽氏が定めた5つの約束事。★2026-08-28時点で実体はどこにも無い（実測）
metadata:
  type: project
---

# イベント運用スキル群（event-*）── 構成と約束事

## いまここ（2026-08-28 23:1x）── ★導入完了。両機で6本が見える

```
配置       ~/vivid-ai-hq/.claude/skills/event-*（6本）＋ EVENT-SKILLS-README.md
           既存8本と衝突ゼロ → 14本。~/.claude/skills はここへの symlink＝全cwdで読まれる
認識       Claude Code のスキル一覧に6本とも出た（実測）
配布       commit → push → mini で merge。★両機とも6本を実測で確認
```

**★動かないものが1つある** ── `event-social-kit` の依存が両機とも不足。

| | MacBook | Mac mini |
|---|---|---|
| Pillow | ✅ | ✅ |
| **cv2 (opencv)** | ❌ | ❌ |
| numpy | ❌ | ✅ |
| **ffmpeg** | ❌ | ❌ |
| Noto Sans CJK | ❌ | 未確認 |

→ **顔ぼかし・リール生成は現状できない。** 他5本のスクリプトは `--help` 起動を実測済み（標準ライブラリのみ）。

**★スキルが呼ぶ他スキルが4本とも存在しない**（実測）── `fukuchi-deck-builder` `docx` `pptx`
`standup-event-notion-importer`。**企画書・報告書の Word/PowerPoint 出力と、放デイ側への
振り分け先が無い。** 参照が空振りするので、出力形式を決める前に有璽氏へ確認が要る。

**★所在が確定（2026-08-28 有璽氏）** ── tar.gz は **claude.ai チャットの添付としてのみ存在**。
Google Drive には一度も置いていない。**＝人が1回ダウンロードするまで実体はゼロ。**
→ [[reference_two_sessions_built_the_same_thing]]「別の面で作られたファイルは、このマシンには存在しない」

**★受け取りと配布の手順（2026-08-28 決定）**

```
1  有璽氏がチャットの添付を ~/Downloads/ へ保存        ← ★人の手が要るのはここだけ
2  こちらが展開 → ~/vivid-ai-hq/.claude/skills/ へ配置
   （~/.claude/skills はここへの symlink ＝置けば全cwdで読まれる。実測済み）
3  commit & push
4  Mac mini は bin/vivid-sync.sh（15分ごと）で自動受信   ★手で scp しない＝二重管理になる
```

**★Drive経由の base64 は採らない。** バイナリは1バイト崩れると展開できず、失敗が静かに残る。
テキストへばらす案（25ファイル）も写経＝転記事故を増やす。**人が1回落とすのが最短で最も確実。**

届くまでスキル本文は読めない＝要約は書けない。**読んでいないものを「把握した」と書かないこと。**

## ★有璽氏の回答（2026-08-29）── 設計の前提が1つ変わった

```
① social-kitの依存      入れる（承認済み）
② Word/PowerPoint/PDF   ★「もあり得る」＝出力形式は固定しない。案件ごとに選ぶ
③ ブランドテーマ        ★イベントごとに異なる。「ふくち。」「NPO」「統合したプロジェクト」等
                        → ★テーマは1つではない。fukuchi-deck-builder 一択の前提が崩れる
④ NotionイベントDB      ★既に在る（有璽氏の明言）。当方は未照会
⑤ 工程の網羅性          ★★下記
```

### ★企画書は必ず作る（2026-08-29 有璽氏・撤回あり）

> 「**企画書作らないと伝えたやつは撤回します。** 簡単にでもいいので、企画書、正式な
> フォーマットでなくても、**情報はしっかりと企画書に落とし込んでいきましょう**。」

- **★下の「企画をやらないケースがある」は撤回された。** 企画書は全イベントで作る。
- **粒度は自由。正式フォーマットでなくてよい。ただし情報は落とし込む。**
  ＝ 体裁を軽くしてよいが、項目を落としてよいという意味ではない。
- これで **KPI目標値の出どころが無くなる問題は消えた**（企画書が必ず在るため）。

### ★出力形式は Word・PowerPoint・PDF を実際に使う（2026-08-29 有璽氏）

> 「**いります。使います。使えるようにして。存在しませんじゃなくて使えるようにして。**」

```
Word        ✅ 既に在る  bin/md2docx.py（python-docx 1.2.0・両機）→ [[reference_md_to_docx]]
PowerPoint  ❌ python-pptx が未導入
PDF         ❌ reportlab/weasyprint/pandoc/LibreOffice すべて不在
動画        ❌ ffmpeg 不在（★「動画は作る可能性がある」＝用意する）
```

**★ブランドテーマは「今のものも使ってよい」**（有璽氏）。テーマはイベントごとに異なり、
「ふくち。」「NPO」「統合したプロジェクト」等が想定される。**単一テーマ前提にしない。**

### ★★台帳起点は原則。ただし全工程を網羅しないイベントがある（2026-08-29 有璽氏）

> 「イベントデータベースを起点に動くという流れは原則ですが、**イベントの種類によっては
> 全てを網羅しないケースもある**。例えば企画をやらないとか。プレスリリースを出さないとか。」

**★スキル側は6工程が一直線に流れる前提で書かれている。** 実際は工程が欠ける。

```
必ず通る    event-db-sync（台帳）  ── 起点であり終点
            event-report-kit       ── ★「報告書の作成等はありますので、そこまでが一体」
                                      有璽氏が名指しで「一体」と述べた唯一の工程

欠けうる    event-plan-kit         企画をやらないイベントがある
            event-press-kit        プレスを出さないイベントがある
            event-social-kit       （同様に欠けうる）
```

- **★「工程を飛ばした」を欠落として扱わない。** スキルは前工程の成果物（企画書のKPI等）を
  引く設計なので、素直に流すと**無い前工程を探して止まるか、勝手に作り始める**。
  **飛ばすと決まっている工程は、着手時に明示して外す。**
- **★ただし報告書までは必ず届かせる。** 企画書が無くても報告書は書く。
  その場合 KPI 目標値の引き継ぎ元が無いので、**目標値をどこで持つかは別途決める**（未決）。
- **★どの工程を使うかは「イベントの種類」で決まる。** 種別と工程の対応表は未作成。

## ★NotionイベントDBの実物（2026-08-29 照会・読み取りのみ）

**正体は 🗓️ イベント管理**（親＝`[12-200]イベント事業部｜tane`）
DB `35a7b156-8b57-83b6-8cfc-8190e06b3366` ／ データソース `collection://b667b156-8b57-8238-b600-07740d7c9f29`
子に **🧩 タスク** `collection://ff77b156-8b57-8356-bc9e-0747af4c8efc`（相互リレーション）。

**★放デイ側は別に実在する** ── `イベントDB｜LIFE STAND UP` `0cbcf694-fea7-4d29-83cc-d396d1704fe2`。
器は在るが `standup-event-notion-importer` スキルは無い。**取り違えないこと。**

### ★実物は9列。スキルは23列を想定している

```
在る(9)   イベント名(title) / 開催日(date・範囲可) / 会場・地域(★select 7択) /
          ステータス(★未着手・進行中・完了の3値) / 主担当(person) / メモ(text) /
          イベント資料(file) / イベント報告書(url) / 関連タスク(relation)

無い(16)  通称 開催時間 所在地 自社の立場 主催者・共催者 参加費 予約 ターゲット
          投稿先アカウント プレス配信 メディア招致 ブランドカラー 掲載NG事項
          来場実績 メディア掲載 振り返り
```

- **★「会場・地域」は欠陥ではない（2026-08-29 有璽氏が訂正）。**
  > 「会場は具体的な場所ではなく、**今はエリアだけを出しています**。
  > 会場名が必要ならその項目も入れてあったら、それはそれでいいと思います。」

  **＝ 意図どおりの運用。既存列は触らず、会場名の列を別に足すだけでよい。**
  ~~正式表記が入らない＝致命的~~ という当初の報告は誤り。**列名が同じでも意味は同じとは限らない。**
  → [[feedback_dont_call_it_a_typo]]「既存の器を『足りない』と決めつけない」
- **「推測してはいけない5項目」のうち4項目は列が無い** ── 参加費・予約・自社の立場・
  主催者共催者。**確認して埋める設計なので、埋める場所を足す必要がある**（会場名も同様に追加）。
- **★ステータスが3値。** スキルは6段階（企画中→開催決定→告知中→開催中→終了→振り返り済）で、
  **「開催決定」を広報の起点にする設計**。実物の3値ではこの起点が表現できない。
- **★KPI／実績の置き場が無い。** 来場実績・メディア掲載・振り返りが無く、
  「台帳へ書き戻して1件を閉じる」が成立しない。**報告書は url 列1本で受ける形。**
- 🧩タスク（8列）も配信タスクDB想定（時刻・フェーズ・媒体・担当アカウント）とズレる。
- **★DBの構造を変えるかは有璽氏の判断。当方は1文字も書いていない。**

## ★実行環境の実測（2026-08-29・両機）

```
✅ 済   numpy / opencv-python を両機へ導入（--user）
★地雷  pip 既定は opencv 5.0.0 が入る → cv2.CascadeClassifier が消えており
        **全モードが起動即エラー**。`pip install --user "opencv-python<5"` で 4.14.0 へ下げて解消
✅ 実測  carousel モードで1枚生成成功（顔ぼかし経路も通った）
❌ 残1   story/reel は落ちる。snspipe.py の FONT_DIR が
        '/usr/share/fonts/opentype/noto' で **Linux 決め打ち**（macOSに無い）
        ＝ **文字を描く全モードが不可**。当方はスキル本体を書き換えていない（要承認）
❌ 残2   ffmpeg が両機に無く brew も無い → **リールMP4のみ不可**
```

**★このスキル群は Linux サンドボックス前提で書かれている。** macOS へ持ち込むと
フォントパスとOpenCV版で必ず止まる。**同種のスキルを受け取ったら、まず1枚通して確かめる。**

## 6スキルの構成（有璽氏の説明。原文ベース・未検証）

| スキル | 役割 |
|---|---|
| event-comms-orchestrator | ハブ。配信カレンダー生成・フェーズ判定・各スキルへの委譲 |
| event-db-sync | Notionイベント台帳への起票・更新・照会 |
| event-plan-kit | 企画書・香盤表・役割分担・KPI設計 |
| event-press-kit | プレスリリース（MARK N式8ステップ）・メディアアプローチ |
| event-social-kit | SNS素材（カルーセル/ストーリー/リール）とキャプション |
| event-report-kit | 実施報告書・KPI実績対比・課題と改善策 |

```
event-plan-kit → event-db-sync → event-comms-orchestrator
                                    ├ event-press-kit
                                    └ event-social-kit
                                          ↓
                              event-report-kit → event-db-sync
```

**台帳が起点であり終点。** 企画書のKPI目標値と報告書の実績値は同じスキーマを共有し、
終了後に達成率が自動で成立する設計。

## ★約束事（2026-08-28 有璽氏。スキルの有無に関わらず効く）

1. **Notionには確認なしに書き込まない。** 新規・更新・ステータス変更のいずれも、応答の末尾で
   「Notionへ最新情報を反映しますか？（対象DB：イベント管理DB）」と確認して承認を得てから反映する。
   **読み取りは確認不要。書き込み前に必ず実データを検索して重複を確認する。**
2. **推測してはいけない5項目**
   ①会場の正式表記 ②参加条件（無料/有料・予約要否）③自社の立場（主催/出展/協力）
   ④併記が必要な主催者・共催者名 ⑤数値（定員・過去実績）
   **★写真や既存資料から読み取れても裏取りにはならない。必ず確認を取る。**
3. **ふくち。グループのブランド資料は fukuchi-deck-builder を使う。配色を独自に決めない。**
4. **放デイ LIFE STAND UP の療育イベントは対象外。** そちらは standup-event-notion-importer が担当。
   **event-db-sync は広報を伴う対外イベント専用。**
5. **イベント写真の肖像権処理は必須。** 自動顔検出は横顔・小さい顔を取りこぼす
   → **生成後に必ず目視確認。プレスリリースに使う写真はSNSより厳しく見る。**

## 参照資料

プレスリリースの型の正典は Google Drive「MARK N広報PR研修」フォルダの**第3回資料**。
8ステップはスキルへ落とし込み済みとのことだが、判断に迷ったら原典を確認する。

## 関連

- [[feedback_press_release_is_not_done_at_distribution]] — 配信で終わりでない（ブログ・SNSまでが1セット）
- [[project_npo_press_releases_202608]] — NPO名義リリース2本
- [[project_pr_agent]] — モルガンズ
- [[feedback_confirm_the_deliverable_form]] — 形式・本数・出口を先に確定する
