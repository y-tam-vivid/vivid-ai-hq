---
name: project_ai_office_console
description: 稼働盤を「見るだけ」から「そこから直接指示できる」AI社員オフィスへ。部品は既に全部ある。未決は指示の自由度1点。
metadata:
  type: project
---

## 有璽氏の要件（2026-09-12）

きっかけは外部LP `ai-work-studio-lp.pages.dev/index-v5-ai-shain`（AI社員6人を研修で組む商材）。
**★有璽氏が求めたのは見せ方ではない。**

> 「こんな感じのものを作りたいっていうのは、**中身がちゃんと動いているもの**っていうことね。
>  今、概念的に言うと、あの Vercel で作っている fukuchi-kadoban と近いとは思うんだけど、
>  **多分このサイトでも直接指示できるみたいな状態**のものになっていると思うので、
>  そこ何がわかったら作れるかな。」

**Why:** 稼働盤は2026-08-23の要件「私が見に行ける状態で」を満たして作った＝**見る専用**。
有璽氏が次に欲しいのは**出す側**。いま指示の入口は ①ターミナル ②Slack のボタン の2つで、
どちらも「一覧を見ながら投げる」ができない。見る面と出す面が別々のままだと、
見て気づいてから投げるまでに人の手が1回挟まる。

**★「中身がちゃんと動いている」が要件の芯。** 絵ではなく、押したら本当に担当が走ること。

## ★2026-09-12 実物を読んだ ── 「動くアプリ」ではなく動画だった

有璽氏から動画の文字起こし（`~/Downloads/ai-shain-office.docx`）とスクショ3枚
（`~/Downloads/AI社員オフィス/`・07:00 / 15:09 / 21:04 の3時点）を受領して確認した。

**★有璽氏の見立て「このサイトでも直接指示できる状態になっている」は、実物では示されていない。**

```
根拠1  スクショ2枚目に動画プレイヤーのシークバーが写っている（01:14 / 全1:40）
根拠2  字幕が焼き込まれている（「届いたDM12件を仕分け、条件に合う3件に返信案を作って、」）
根拠3  LP本文も「▶ 実際に働いている様子（画面デモ・約1分40秒）」＝デモと明記
根拠4  ★「社長席／承認」「掲示板／明日の指示」は3枚とも同じ絵のまま。押された形跡が無い
```

**★示されているのは「見る画面」だけ。指示の口は絵として置いてあるだけ。**
裏返すと、**うちが作れば本物になる**（中身が実際に動いているのはこちら）。

### ★芯は「持ち場」ではなく「リレー」（動画の第一声）

> 「役割ごとのAI社員が、**1つの案件をリレーで仕上げる**仕組みです」

実況ログがそれを表現している ──
「3行にまとめカクの席へ」→「カクが受け取り✓」→「返信案と反応をセイの席へ」→「セイが受け取った」。
**★うちのいまの形は14体が独立に自分の持ち場を回す構造で、ここが違う。**
成果物が担当から担当へ渡る形をどう表すかが、設計の分岐になる。

### 読み取れた画面の構成（そのまま設計に使える）

```
ヘッダー      案件名 ＋ ★現在時刻（大きく）
フロアマップ  2×3の机。各机＝役割名／名前／★いま持っている成果物カード（「話題 3行」
              「投稿文 台本1本」「DM 12件」等）／立ち絵／机上の進捗バー
              ★動いている担当は枠が発光＋吹き出し「…」
足元          ★社長席（承認ボタン）／★掲示板（明日の指示）  ← 有璽氏が欲しいのはここ
右ペイン上    「いま起きていること」＝時刻＋一文の実況ログ・★最新行をハイライト
右ペイン下    いま作業中の担当カード（担当/役割・やっていること・★進捗バー・★％）
最下部        06:00→22:00 の一日の帯。★「あなたの15分」「あなたの30分」をオレンジで
```

### ★うちに無いもの3つ（ここが実装の焦点）

| | いまの状態 | 難度 |
|---|---|---|
| ① 進捗％（作業中68%） | **持っていない。** `claude -p` は進捗を返さない | ★最難。担当に工程を宣言させる形が要る。**偽の％は出さない**（[[feedback_never_write_an_unmeasured_number]]） |
| ② 成果物カード（誰が何を持っているか） | `run_agent_launch.log` と出口ファイルから作れる | 中。出口ファイルの命名を揃える必要 |
| ③ 掲示板＝指示の入口 | 無い。Slackで判断待ち #a9a97b | 中。lsu-editor の Blob 経路を流用 |

**★フロアマップの半分は既にある** ── 稼働盤に「エージェント稼働アイコン行」が実装済み
（2026-09-07 ピタゴラス・走行中=緑枠+緑ドット点滅／停滞疑い=赤／待機=グレースケール）。
アイコンは `~/.vivid-relay/assets/agents/<agent_key>.png` に13体ぶん実在（フランキー作）。

**★机を何で並べるかが未決**：うちは6人でなく14体、案件も複数同時に走る。
2×3のフロアマップにそのまま載らない。
（案1 担当14体を常設して動いている者だけ光らせる ／ 案2 いま走っている案件ごとに机を並べる）

## ★2026-09-12 有璽氏が方向を確定させた（会話で回答・#a9a97b はボタン未押下）

### ① 誰のための画面か ── 優先順位が3段で示された

> 「**基本は私が使うためのものというのが最優先**で、社員、社内にも使える形なのであれば
>  そのようにします。その上で**外部に提供していけるのであればそのようにしていきたい**。
>  ただ、外部に共有提供するのであれば、**福祉**かな、福祉の**施設の事務**だったり、
>  こう、いろいろやるべきこと、1日業務でもあるので、そういったものを
>  **こういう会議室のような形**にして、動いている状況を見せていくような、
>  そういうものを作りたいと思います。」

```
第1  ★有璽氏本人が使う        ← 設計はここに最適化する。迷ったら本人の使い勝手を採る
第2  社内・社員も使える形なら  ← 第1を壊さない範囲で
第3  外部提供                  ← ★対象は福祉施設の事務。1日の業務を会議室の形で見せる
```

- **★「会議室」という語は有璽氏のもの。**「オフィス」「ダッシュボード」へ勝手に言い換えない
- **★外部版の中身は既に手元にある** ── 福祉施設の困りごと118件・業務268件（自社130/外部138）・
  価格98件のスプレッドシート（2026-09-08 作成）。**外部版の「持ち場」はここから起こす。
  1から棚卸しし直さない** → [[feedback_break_down_to_the_work_level]]
- **★外部へ出すなら情報ファイアウォールが効く。** 社内の数値・顧客名・個人情報は出さない

### ② 指示の出し方 ── ★Slackと画面の両方。★スマホ必須

> 「Slackとこの会議室にメッセージを送る、からメッセージを送れるような設計にする。
>  **その両方ともで指示が出せるような状況**にしておきたい。なので、**外出先等でスマホからでも
>  指示ができるような形、進捗が見えるような形**です。」

```
★入口は2つ      Slack（既存 ask_hub / slack_inbox）  ＋  会議室の画面（新規）
★台帳は1つ      ask_hub_queue.json へ寄せる
                 ＝入口が2つでも二重管理にならない（規範「二重管理が増えないか」を満たす形）
★形は「メッセージを送る」＝ 実質 C（自由文）。A/B/C の3択のうち C 寄りの回答
★スマホで指示できる ＋ ★スマホで進捗が見える  ← ここが設計の最大の制約
```

**🔴スマホ必須がフロアマップ案を壊す。** 動画の2×3の机＋立ち絵は、スマホ幅では成立しない
（机が縦に6つ並ぶと1画面に入らず、成果物カードも読めない）。**画面は2層で設計する。**

```
PC / タブレット   会議室ビュー（机を並べる・立ち絵・実況ログを右に）
★スマホ          ①いま動いている担当だけを縦リスト ②実況ログ ③メッセージ入力欄
                  ★会議室の絵は縮約する。無理に机を並べない
```

**★Cを採るなら、先に言った代償が現実の課題になる** ── ブラウザ＝実行権限。
いまは合言葉1本しか挟まっていない。**外出先のスマホから開く＝端末が増える**ので、
人ごとのログインと危険語の検問は「後で」ではなく**最初から**要る。

### ★#a9a97b の扱い

ボタンは押されていない（`answer_of("a9a97b")` が `null` を実測）。
**有璽氏が会話で明確に答えたので、その発言を根拠に当方で閉じる。**
★AIの判断で畳んだのではない（過去に #960e67 で「AIが有璽氏の判断を代行して畳んだ」と
つるに指摘された型を避ける）。閉じるときは回答者と根拠を台帳へ明記すること。

## ★2026-09-12 担当14人の立ち絵を作った（$1.86・有璽氏の許可あり）

**★既存の `~/.vivid-relay/assets/agents/*.png`（13体・フランキー作）はキャラではなく記号アイコン**
（コンパス等の平面シンボル）。実物を開いて確認した。**会議室には使えない**ので作り直した。

```
道具      bin/gen_images_openai.py（既定はドライラン。--run のときだけ課金）
指示      scratchpad/office_agents_prompts.json  ★共通指定1本 ＋ 枠ごとの差分14件
          （枠ごとに画風を書き分けると揃わなくなる ── 2026-09-08 かわちばなしの原因）
費用      概算 $1.86／実測 API 40回（再試行込み）。gpt-image-1.5 / high / 1024x1024
置き場    ★両機 ~/.vivid-relay/assets/agents_portrait/（原寸）と .../320/（表示用320px）
          ★scratchpad は揮発する。金を使って作ったものは必ずホーム配下へ移す
```

**★「合格 2/14」は判定基準のミスマッチ。**`gen_images_openai.py` の合否は
かわちばなしの**風景写真用**（明るさ60〜70・彩度35〜45）で、**白背景の立ち絵には当たらない**。
スクリプト自身が最後に「★数値が通っても必ず実物を目で見てから採用すること」と出す。
**★14枚を1枚のコンタクトシートに並べて目視し、画風の統一と小道具を確認して採用した**
（3枚だけ見て「揃っている」と断定しない → [[feedback_one_route_is_not_verification]]）。

- **★立ち絵は白背景で出る。**暗い会議室に置くと枠が浮くので、**枠の地も明るくする**
  （`object-fit:contain` ＋ 明るいグラデ）。消灯している席はグレースケール＋不透明度で落とす
- **★原寸1024pxのまま載せない。**表示は76px。`sips --resampleWidth 320` で 18MB→1.7MB
  （→ [[feedback_artifact_accumulate_dont_replace]]「縮小版と必ず言う」）
- **🔴残る偏り** ── 14人中、女性に見えるのは2〜3人。**性別を指定していないので生成側の傾向がそのまま出た。**
  直すなら差分プロンプトに明記する。★有璽氏の判断待ち

## ★2026-09-12 第2版への追加要求5点（有璽氏）

> 「もうちょいなんか**実像に即したい**なぁ。さっきよりはいいようになった気がする。
>  ただこれなんか、**動いてるのを視覚的にわかるようにしてほしい**なぁ。**どこからどこに**みたいな感じで。
>  あとなんだろう、見える化で言うと、**ルーティンになってるやつとか、そういうタブ**かな、
>  みたいなのを作って、**それが動いたかどうか**みたいなのを確認できるような設計にして。
>  それと、**Slackで通知未承認みたいな風になってるやつがあるんだけど、それもどれかわからんから、
>  こっちでも押せるようにしてほしい**わ。**同じ内容**をね。**どっちかで押せばいけるみたいな形**にして。
>  **Slackで伝えるも含めて**ね。あと今は単純にイラストがあるだけなので、
>  この場では不要だけど、**クリックしたらその人の動きが追えるようにして、
>  この動きをした、この動きをした、みたいな、この時間に**、みたいな形にしてほしい。」

```
① 実像に即す       ★手で起こした静止版をやめ、実データから組む
② リレーの可視化    ★「どこからどこに」＝成果物が誰から誰へ渡ったかを線で見せる
③ ルーティンのタブ  ★定期実行40本を一覧にし、★今日動いたかどうかを1つずつ出す
④ 承認をこちらでも  ★Slackの未承認と★同じ内容を会議室にも出す。
                    ★どちらで押しても成立する（＝台帳は1つ・[[project_ask_hub_push_decisions]]）
                    ★「Slackで伝えるも含めて」＝会議室で押した結果もSlack側へ反映する
⑤ 人をクリック      ★その担当の「この時間にこの動きをした」を時系列で開く
```

### ⑥ 追加（同日）── 「通知しましたよ」を別で見せる

> 「**通知しましたよ、みたいなのも出してほしい**かな。特にビビとかロビンもか、
>  なんかそういう**毎日固定であるやつ**は、その例えば**Slackとかに出してくれてるやつ**とか、
>  ああいうのも**そういうのをなんか見えるようにしてほしい**ですね。**別でね**。」

**★「別で」＝会議室の実況ログとは分けたタブ。**
実況ログは「社内で何が動いたか」、通知タブは**「外（Slack）へ何を出したか」**。
**★この2つは別物。混ぜると、出したつもりで出ていない事故が埋もれる**
（→ [[feedback_write_for_the_reader]]「送った≠届いた」）。

```
★見せるもの  いつ・誰が・どこへ（DM/チャンネル）・何を・★出したのか出さなかったのか
★特に大事    「変化なしのため送らなかった」を★同じ列に並べる
              （2026-09-12 の progress_report がまさにそれ。送っていないのに
                ログ上は正常に見える ＝ 有璽氏から見れば「進捗が見えない」の正体）
```

### ⑦ 追加（同日）── AIの使用量を見たい。★調査結果＝取れるものと取れないものが割れる

> 「使ってるAIの全部見れたらいいなと思うんだけど、クロードのアカウント、ChatGPTとかもそうだし、
>  あと僕だと**Manus**とかもあるんだけど、それの**それぞれの使用量**を知りたいです。
>  **何パーセント使ってるのか**とか、**いつリセットされるのか**とか。
>  それが**それぞれのエージェントでどれぐらい消費しているのか**とかも見えるようにしてほしい。
>  それを後で**CSVファイル**、CSVじゃなくてもいいけど、何かしらでアウトプットして**検証できるように**もしたい。」

**★実測（2026-09-12）**

```
✅ 取れる   Claude Code のトークン消費 ── ★transcript の usage 欄に実在
              ~/.claude/projects/**/*.jsonl（★125ファイル）
              {"input_tokens":2,"cache_creation_input_tokens":72971,
               "cache_read_input_tokens":24818,"output_tokens":344,...}
            ★担当ごと・日ごとに集計できる＝「エージェントごとの消費」に直接答えられる
            ★CSV出力もできる（読むだけ・可逆）
✅ たぶん   Manus のクレジット ── 鍵は ~/.config/manus/api_key に実在。★APIで残量が返るかは未確認
✅ たぶん   OpenAI の使用量 ── OPENAI_API_KEY は config.env に実在。usage API を叩けば取れるはず

🔴 取れない Claudeプランの「何％使った・いつリセット」
              ★ANTHROPIC_API_KEY が無い（config.env に0件）＝Admin/Usage API を叩けない
              ★そもそも Max プランの消費率はローカルに保存されていない
              ★見られるのは Claude Code の /usage コマンドだけ＝★有璽氏が自分で叩く領域
🔴 取れない ChatGPT Plus（サブスク）の使用量 ── APIで公開されていない
            ★APIキー経由の従量課金分とは別物。混ぜて出すと嘘になる
```

**★「％」だけを目的にすると全部できないことになる。**
トークン実数は取れるので、**「担当ごとの消費トークン数と概算金額」を正とし、
プランの残％は“ここでは出せない”と明記する**のが正しい形。

### ⑧ 🔴「時間はどこに出てんの？」── 代替案を言って出していなかった

> 「パーセンテージは出せないって書いてるけど、**じゃあ時間はどこに出てんの？今**。
>  …**終わり時間とかは出すって言ってたよな。なんかそれは今見えへんねんけど。**」

→ [[feedback_stop_asking_just_do_it]]「代わりにこれなら出せると言ったものを、出していなかった」

**★出せると分かった実データ**

```
担当(claude -p)の所要時間   ★run_agent_launch.log に 起動/完了 が対で残っている
                            実績6件＝4分／13分／21分／36分／36分／69分（★中央値28分）
                            → 走り出したら「終わる見込み ○時○分ごろ」を出せる
定期実行の所要時間           ★1分未満（起動と完了が同じ分に記録される）
                            → 意味があるのは「終わり」ではなく★「次にいつ動くか」
★新しく分かった事実          日次ジョブは★定刻に動かない。15分おきの巡回で拾う仕組み
                            実測 07:50→08:00 ／ 08:15→08:30 ／ 09:35→09:45（★10〜15分遅れ）
                            ★「定刻」を予定として見せると毎日ズレて見える。
                              「この時刻以降、最初の巡回で動く」と書く
```

**★「どこからどこへ」はルーティンと別物ではない**（有璽氏が混乱した点）。
40本のうち**前の結果を次が受け取る3本**を渡る順に並べ直しただけ。画面にそう明記した。

**★④がいちばん重い。**「押せる形」ではなく「押したら本当に効く」が要件。
絵のボタンを置いてはいけない（[[feedback_read_the_artifact_not_the_copy]]「絵を本物のように見せない」）。
**★台帳は `ask_hub_queue.json`（mini）1つのまま。入口だけ2つにする。**
双方向に書く設計にすると、2026-09-12 に見つけた
「`close_without_answer()` が台帳を閉じない」型の食い違いが倍に増える。

**★②は前提の確認が要る。**うちの定期実行40本は**持ち場型で、担当間の受け渡しが無い**。
リレーが実際に起きるのは、担当（`claude -p`）を投げたとき（ビビ→リリス→つる→公開）。
**★「今日はリレーが0件」という日がありうる。**その日に線を描くと嘘になる。
実データに無いものを描かない → [[feedback_never_write_an_unmeasured_number]]

## 🔴2026-09-13 「止まってるのは仕様なの？」── 静止版の時計が現在時刻に見えた

> 「これ、16時40分、**昨日の16時40分から止まってる**のは、わざと何かこちらが指示しないと
>  止まっちゃう仕様なの？」

**★担当が止まっていたのではない。画面が更新されていないだけ。**
会議室は手で起こした静止版で、時計の「16:40」は**データを取った瞬間を焼き付けただけ**。
それが**画面のいちばん目立つ場所に、現在時刻の見た目で出ていた。**

```
★何が悪かったか
  ① 大きな時計が「16:40」とだけ出ていた ＝ 現在時刻としか読めない
  ② 「自動更新はまだしていません」は書いてあったが★画面のいちばん下
     ＝ 目に入る場所と、読ませたい場所が違った
  ③ ★経過時間を出していない。「17時間前のデータ」と書けば一目で分かった
```

**★これは既に記録してある型の再発**（[[project_ops_dashboard]]）──
「★静的HTMLは黙って古くなる。生成時刻を焼くだけでは『古い版を最新と思って読む』事故」。
**記録はあった。読んで作らなかった。**

- **★古さは「注記」ではなく「状態表示」にする。**小さく添えるのではなく、
  **鮮度そのものを画面の要素として出す**（何時間前か・自動更新しているかどうか）。
- **★数字を大きく出すときは、それが「いつの値か」も同じ大きさで持たせる。**
  大きい数字ほど現在の値だと読まれる。
- **★Artifact は原理的に自動更新できない**（Artifactツールからしか publish できず、
  cron からも GAS からも叩けない → [[project_ops_dashboard_artifact]]）。
  **自動更新が要るなら Vercel へ出すしかない**（稼働盤と同じ経路・30分おき）。
  ★つまり「実像に即す」と「外でも見える」は同じ工事になる。

## ★2026-09-13 更新間隔を確定（有璽氏）── Vercelへ出す作業に着手

> 「自動で更新が継続的にかかる形でないと、この設計そのものが意味ないよな。これなんかそういうのめちゃくちゃ多いからさ。なんで止まってんのって話やし、止まらんように設計して。」
> 「結局タイムリーに反映させることはできないんでしたっけ、これ。」
> （経過報告を受けて）「この経過で大丈夫です。画面の更新30秒、担当の動き出しは即時。レジスタを30分でOKです。」

**Why:** Artifactは publish が会話からしか叩けず cron から叩けない＝★原理的に自動更新できない
（[[reference_artifact_is_not_public]] とは別の制約＝こちらは「更新できない」、あちらは
「他者に届かない」）。会議室が静止版のままだった 2026-09-12〜13 の「なんで止まってんの」は
この構造上の限界が正体だった。**How to apply:** 自動更新が要る画面は最初から稼働盤と
同じ Vercel + Blob 経路で作る。Artifact は「1回見せる」用途に限定する。

```
★確定した間隔（種類ごとに別々。全部を30秒で取りに行かない＝Vercel日次100デプロイの節約）
  画面の更新       30秒   Blob を fetch するポーリング（稼働盤rtPollと同じ実装を流用）
  担当の動き出し    即時   run_agent.sh の起動/完了の瞬間にBlobへ1行書く（ポーリングは変えず
                         書く側を速くする＝体感で即時にする）
  レジスタ          30分   Notionは人が書く欄。秒単位で叩いてもレート制限と費用が増えるだけ
```

**✅実装完了（同日・ピタゴラス）**。`~/vivid-ai-hq/web/kadoban/office` （既存の稼働盤サイトへ
相乗り。新しいVercelプロジェクトは作っていない）で開ける。詳細・実測値・限界は
`WORKING.md` の同名ブロック（2026-09-13・✅完了）と `~/.vivid-relay/office_vercel_result.md` 参照。

**★重大な実測 ── Vercel BlobのGETにはCDNキャッシュがあり、「即時」は文字どおりの
即時にはならない。** 既定（cache-control-max-age省略＝30日）だと反映まで数分単位で
古い値が返り続ける。`--cache-control-max-age 0` を指定して初めて8〜25秒程度まで縮む。
**次にBlobへ書き込む処理を作る人は、この引数を必ず指定すること**（稼働盤の
`dashboard_realtime_push.py` は指定していない＝30分おき更新だから気づかれていないだけで、
同じ遅延を抱えている可能性が高い。触る機会があれば合わせて直すこと）。
## ★2026-09-13 「タイムリーに反映できないんでしたっけ」── できる。30秒まで行ける

**★答え：できる。ただし場所を変える。**

```
いまの会議室（Artifact）   ★原理的に自動更新できない
                           publish は会話からしか叩けず、cron からも GAS からも叩けない
Vercel へ出す              ★30秒ごとに数字が変わる。★稼働盤が既にやっている（実証済み）
```

**★部品は全部ある。1から作らない。**

```
/api/data              Blobから読む Vercel Function     web/kadoban/api/data.js
30秒ポーリング          画面側 setInterval(rtPoll,30000) dashboard_build.py:273-340
Blobへpush             dashboard_realtime_push.py
デプロイ                bin/kadoban_deploy.sh（★同じプロジェクトへ相乗り。新規に作らない）
Basic認証               web/kadoban/middleware.js（既に効いている）
Notionレジスタの読み取り  ★mini から API で読める（heartbeat.py が NOTION_TOKEN で実証済み）
                        ★MCPは無いがAPIは叩ける。ここを「読めない」と誤判定しないこと
```

**★「タイムリー」の限界は画面側ではなく元データ側にある。**

```
画面の更新        ★30秒（ポーリング）
担当が走り出した   ★即時にできる（run_agent.sh が起動時に Blob へ書けばよい）
定期実行の結果     最短5分（stall_watch・slack_inbox が5分おき）
レジスタ（Notion） 30分おきに取りに行けば十分（人が書く欄なので秒で変わらない)
```

**★🚦状態（Notionの計算列）は API から取れない。★自分で再実装しない。**
「最終結果」と「最終実行＋期待間隔の超過」を別々に出して画面で並べる
（[[project_ops_dashboard]]「数はレジスタと画面の2箇所で数え、食い違ったら画面に出す」）。

**★この工事は「実像に即す」と「外でも見える」を同時に解く。**別々の課題ではない。

### ✅更新の間隔は有璽氏が確定させた（2026-09-13）

> 「この経過で大丈夫です。**画面の更新30秒、担当の動き出しは即時。レジスタを30分でOK**です。」

```
画面の更新       ★30秒   Blob を fetch するポーリング（稼働盤と同じ）
担当の動き出し    ★即時   ★run_agent.sh が起動した瞬間に Blob へ書く
                         ＝「誰かが走り出した」が30秒以内に画面へ出る
                         ★ポーリングを速くするのではなく、書く側を速くする
レジスタ          30分    Notion は人が書く欄なので秒では変わらない
```

**★「タイムリー」は一律の数字ではない。**データの種類ごとに、
**変わる速さと、知りたい速さ**で別々に決める。全部を30秒で取りに行くと、
Notion API を無駄に叩き、レート制限と費用だけが増える。

**★速くするのは「読む側」ではなく「書く側」。**
担当の起動は1日に数回しか起きないので、ポーリングを速くしても意味がない。
**起きた瞬間に書けば、遅い読み取りでも即時になる**（push と pull を混ぜない）。

## How to apply ── 部品は全部ある。1から作らない

```
見る側（稼働済み）
  https://fukuchi-kadoban.vercel.app  Basic認証・middleware.js
  web/kadoban/api/data.js             Vercel Function（λ api/data・★拡張子なしでマッピング）
  dashboard_{data,projects,build}.py  mini の cron :03/:33 で出し直し
  dashboard_realtime_push.py          Vercel Blob へ数字を push → 画面が30秒ポーリング

★指示側（既に1本通っている＝ここを流用する）
  lsu-editor（2026-09-10・実測8項目合格）
  ブラウザ → Vercel Function → ★Vercel Blob へキュー保存
            → ★mini の editor_apply.py が cron */5 で読む → 実行 → 出し直し
  体感1〜6分で反映。★この形を「CSSを書き換える」から「担当を起動する」に差し替えるだけ

起動側（稼働済み）
  ~/.vivid-relay/run_agent.sh <担当名> <指示文ファイル> [作業ディレクトリ]
  AskUserQuestion封じ／認証確認／切り離し（ssh切断でも死なない）／通信系のみ再試行3回／
  ログ統一／run_agent_launch.log へ1行台帳

判断側（稼働済み）  ask_hub.ask() ── Slackのボタン・台帳・answer_of() で回収
見張り（稼働済み）  stall_watch.py（*/5・無言20分で鳴る）／心拍→⚙️自動処理レジスタ
```

### ★Blob 経路の既知の穴（2026-09-10 実測・そのまま効く）

- **書き込みが `@vercel/blob` SDK の非公開ヘッダーに依存**（`x-vercel-blob-access` 等）。
  公式ドキュメントに無い＝**SDK更新で壊れる可能性**。壊れたら指示が黙って届かなくなる
- **put 直後の get で古い内容が返ることがある**（1秒待って再送する対策済み）
- → **指示コンソールに流用するなら「投げた指示が届いたか」を画面へ返す経路が要る**
  （投げっぱなしにしない。ask_hub と同じ「出した側が台帳を閉じる」型）

## ★未決は1点だけ ── 指示の自由度

技術の不明点はほぼ無い。作るものが根本から変わるのはここ。

| | 何ができる | 代償 |
|---|---|---|
| A ボタンだけ | 決まった処理を起動（台帳を数え直す・稼働盤を出し直す・担当の定型タスク） | 新しい仕事は投げられない |
| B 定型＋入力欄 | 担当を選び、対象を入れて投げる | 入力の検査が要る |
| C 自由文 | 画面に書いた文がそのまま `claude -p` へ | **ブラウザから任意の処理が走る。合言葉1本の内側で** |

**★Cは「ブラウザ＝実行権限」になる。** Basic認証1本しか挟まっていない現状では、
稼働盤のURLと合言葉を知る者が mini 上で何でも動かせる状態と同義。
段階導入（A→B→C）が既定の推奨。

## こちらで既定を決めてよい4点（有璽氏に聞かない）

1. **反応速度** ── まず5分ポーリング（既存 cron を流用）。数秒が要るなら後から常時接続へ
2. **承認の置き場** ── 判断は Slack のボタンのまま（[[project_ask_hub_push_decisions]]）。
   画面にも置くと二重管理になる。画面は「投げる」だけ、「決める」は Slack に寄せる
3. **誰が開くか** ── まず有璽氏のみ（現行の Basic 認証のまま）
4. **失敗の戻り先** ── run_agent.sh のログ ＋ stall_watch ＋ Slack。新しい見張りは作らない

## 関連

[[project_ops_dashboard]] 稼働盤の本体 ／ [[project_web_build_rules_asset]] lsu-editor の Blob 経路 ／
[[project_ask_hub_push_decisions]] 判断はボタンで返す ／ [[reference_offload_long_work_to_mini]] 長い処理は mini へ


## ✅2026-09-13 ①〜⑤を通した（有璽氏「1から順に5番まで」）

**有璽氏の2つの苦情には、別々の真因があった。**

```
「押しても更新するたび毎回戻る」
   answer() が claude.use("db") 前提。★Vercelでは DB=null なので、押した結果は
   ページ内の `answers = {}` にしか残らない＝★どこにも届いていなかった
   → 画面 → /api/office-answer → Vercel Blob → office_answer_apply.py(cron */5)
     → ask_hub_queue.json。★Slackと同じ1本の台帳が閉じる（Slack側のボタンも消える）

「リリスが動いているのに0人と出る」
   `ps -eo pid,etimes` は macOS に etimes が無い。★警告を出して rc=0 で返る。
   列が減った出力を読むと全行が黙って捨てられる。★同じ穴が3本にあった
   （office_data / vivi_patrol は今回修正。stall_watch は9/5に修正済みだった
     ＝★記録は在ったのに、後から書いた2本が同じ穴に落ちた）
```

**★正本を1本にした（②）** `~/.vivid-relay/agent_running.py`。
会議室（office_data）も羅針盤（dashboard_projects）もこれを読む。
それまでは★3通りが別々に計算していた（ps／psのペルソナ宣言の正規表現／静的な名簿）。
実測：プローブ走行中に 会議室 count=1 と 羅針盤 web-developer が同じ瞬間に一致。

**★判断待ちは「承認」より広い（③）**
```
ask_hub の台帳（押せる）      open ★0件   ← 承認だけ並べても1件も出ない
WORKING.md の判断待ちの記述   ★50件       ← 実体はこっち。誰にも届いていなかった
```
`pending_decisions.py` が拾う。★これは「候補」で、件数は正確でないと画面にも書く
（言い回しで拾い落とす。機械の件数をそのまま「決めるべき数」として出さない）。

**★成功率の出し方（④）** 直近7日 起動89・終わった83・rc=0が83。
★rc=0は「最後まで走った」であって「意図どおりできた」ではない（2026-09-07 の実例）。
画面にもそう書いた。★起動と終了の差（6件）も出す＝率だけ見せない。

**★⑤は既に動いていた。**`running_pinged_at` が実測で更新されている。
前に「未検証」と書いたのは★CDN経由の古い版を読んでいたため。

**🔴 Blobは push 直後に古い版を返す。**private ホストへ Authorization 付きで直接読んでも
数十秒は前の中身が返る（x-vercel-cache: HIT）。★1回読んで「入っていない」と断定しない。


## 🔴2026-09-13 押しても届かなかった（原因の切り分け）

有璽氏「押せるものは押しました」→ **実測ではどこにも届いていなかった。**

```
/api/office-answer へのPOST   ★0件（Vercelのランタイムログで確認）
ask_hub の台帳                 ★2件ともopenのまま（回答の記録なし）
slack_socket                   ★生きている（9日18時間稼働・hello受信済み）
＝ Slackでも会議室でも、確定した回答は1件も無い
```

**★いちばんありそうな筋**：有璽氏が見た 14:31〜14:33 の画面は、まだ**承認0件**だった。
Blob へ push したのが 14:30 で、**画面が読む口（/api/office）に新しい版が出るまで数分かかる**
（同日に実測：x-vercel-cache: HIT / age=808 で 13分前の版が返っていた）。

**★だから手順を変える**
```
① 判断を発行する（台帳へ入る）
② Blob へ push する
③ ★受け取る側（/api/office が返す中身）を読み返して、承認N件が出ているのを確かめる
④ そこで初めて人へ「押してください」と言う
```
★③を飛ばすと、人に空振りさせる。2026-09-13 は同じ日に2回やった。

**★ついでに分かったこと**：立ち絵の無い担当は `/chars/<key>.png` が404になる
（ログに `responsive-engineer.png` の404が2回）。画面は頭文字で出るので壊れないが、
**404はログに残る＝立ち絵を足すまでノイズが出続ける。**


## 🔴2026-09-13 ボタンが500を返していた ── 真因は「書き込みだけが依存を要る」

**★有璽氏が実際に押して、画面の赤字が真因を教えてくれた。**
```
★届いていません： Unexpected token 'A', "A server e"... is not valid JSON
```
`A server e…` ＝ Vercel の 500 本文 "A server error has occurred"。
JSONでない本文を `r.json()` が読もうとして落ちた。**＝Functionは呼ばれていた。**

**真因**：`office-answer.js` の `import { put } from '@vercel/blob'` が解決できなかった。
公開先に `package.json` が無かった。

★**なぜ今まで気づけなかったか** ── 既にある2本（`data.js` `office.js`）は
**読むだけで、fetch しか使わない＝依存が要らない**。だから package.json 無しで動いていた。
**書き込みを足した瞬間に初めて依存が要る**（直接HTTP PUTは403/400で通らないと
lifestandup 側で実測済み・SDK経由が唯一の経路）。

**実測（2経路）**
```
押して500になった版(e7milcv7s)  ビルドログの "Installing dependencies" ★0回
package.jsonを入れた版(gh90m600e) 同 ★1回（added 31 packages）
```

**★同時に直した2点**
- 届かなかった回答も承認の件数から引いていた＝**押したら消えたように見える**。
  → ★届いた回答だけ引く。届かなかったものは ⚠ と枠の色で分かる形にした
- ヘッダーの「社内のAI担当 14人」が**固定値**だった（席は15人）。→ 実数から出す

**★教訓**：画面に「黙って成功したように見せない」を入れておいたから、
有璽氏が1回押しただけで真因まで辿れた。**赤字で理由を出す設計は、検証の道具になる。**


## ✅2026-09-13 ★通った ── 画面のボタンで押した回答が、Slackと同じ台帳を閉じた

```
14:56:53  有璽氏が会議室のボタンを押す（2件）
          → /api/office-answer → Blobのキューへ ★by=「会議室（画面のボタン）」
14:57:18  mini が拾って ask_hub_queue.json へ answered として確定
          → ask_hub._finish が Slack の元メッセージのボタンも消した
          → open 0件
```
**★経路が全部つながった。** 画面で押してもSlackで押しても、同じ1本の台帳が閉じる。

**★その場で直した小さな穴2つ**
- `vercel blob put` に `--access private` が要る（無いと
  「Missing required --access flag」で applied 印を書き戻せない）
- 既に回答済みのものは `_finish` が弾く＝★二重には入らない（実測）

## 🔴 立ち絵の合否判定が、既存の絵柄に合っていなかった

チョッパーの立ち絵を作ったら**3回とも不合格**（彩度13.1/17.3/16.7・基準は彩度18-60）。
★そこで**既存14枚を同じ道具で測り直した**：

```
合格 ★2/14 のみ。彩度は 10.3〜21.7（中央値 約14.5）
```
**＝基準の方が既存に合っていない。** この基準は「かわちばなし」の★写真向けに作ったもので、
★3Dの立ち絵には合わない。新規の3枚は既存のど真ん中だった。

**★だから数値でなく実物を見て採用した**（規範どおり）。粘土質感・3頭身・配色・背景とも
既存と同系統で、ものさしを持つ仕草で役割が一目で分かる。

★**次に立ち絵を足す人へ**：`gen_images_openai.py` の合否は写真用の基準。
立ち絵では★不合格が出て当たり前。**既存を同じ道具で測って並べてから判断すること。**
（費用：API 3回で概算 $0.40。基準が合っていれば1回 $0.13 で済んだ）
## ✅2026-09-13 Vercelへ出た ── 自動更新が始まった

**★ https://fukuchi-kadoban.vercel.app/office** （合言葉は稼働盤と同じ）

```
画面の更新       30秒   Blobを fetch するポーリング
担当の動き出し    ★8〜25秒（実測）  run_agent.sh が起動/終了の瞬間に Blob へ書く
レジスタ          30分   既存の稼働盤 cron（3,33）へ相乗り。★新しいcron行は作っていない
```

**作られたもの**（mini・実測でファイルの実在を確認）

```
~/.vivid-relay/office_data.py          実データ収集（★読むだけ）
~/.vivid-relay/office_build.py         HTMLを組む（★Artifact版の見た目とdraw関数はそのまま）
~/.vivid-relay/office_realtime_push.py Blobへpush
~/.vivid-relay/office_running_ping.py  ★担当の起動/終了を即時にBlobへ
web/kadoban/api/office.js              Vercel Function（data.js と同型）
```

**★検算（作った本人の申告を検査にしない）**

```
合言葉なし      / も /office も 401 ＝ 中身は認証の向こう側
実データ        office_data.py を実行 → 担当14件・ルーティン55件・承認待ち2件・生成時刻つき
既存の稼働盤    ★無傷（10:03 のデプロイが成功・401を実測）
crontab         ★既存の 3,33 行へ相乗り。新しい行は増えていない
```

### 🔴担当が見つけた穴 ── Vercel Blob の GET には CDN キャッシュがある

**既定のままだと反映が数分遅れる。**`--cache-control-max-age 0` を付けて 8〜25秒まで縮めた。
**★「即時」ではない。**8〜25秒という実測値をそのまま持つ。

**★同じ穴が稼働盤側（`dashboard_realtime_push.py`）にもある可能性がある。**
稼働盤は「30秒おきに数字が変わる」と謳っているが、**Blobのキャッシュで実際は数分遅れているかもしれない。**
★未確認。次に触る人が実測すること（push した時刻と、画面に出た時刻の差を測る）。

### ★AIが測れないこと（人の領域）

**合言葉ありで実際に表示されるかは、AIからは確かめられない**
（Vercel の Secret は `vercel env pull` が `[SENSITIVE]` を返すため）。
→ [[project_ops_dashboard]]「AIが測れるのは『合言葉なしで401』まで」。**有璽氏の目視が要る。**

## 🔴2026-09-13 ★「私の判断待ち」というカテゴリを作る（承認より広い）

> 「台帳については別案件で進めようかな。これもどっかに書いといてくれたらいいかな。
>  **私の判断待ちみたいなやつ。これ作ってくれてたっけ？そのカテゴリーとして。**
>  なんかあった気がする。**承認だけ。承認か。まあ承認でもいいけど、
>  私の判断待ちみたいなやつを作って。**」

**★「承認」と「判断待ち」は同じではない。**いまの器は承認しか持っていない。

```
★いま在るもの（実測）
  ask_hub_queue.json      ★承認＝ボタンで押すもの。Slackへ投げたものだけが入る
  build_pending_human()   ★在った。dashboard_data.py:496
                          ★WORKING.mdの表からキーワードで拾うだけ（最大15件）
                          ★稼働盤の「仕組み」タブに出る＝★埋もれている
                          ★読むだけ。押せない。★会議室には出ていない
  Notion ③決定・論点ログ   未決論点。★人が書きに行く器＝自動では溜まらない
★足りないもの
  ①この3つが1か所に集まっていない（承認／WORKING.mdの止まり／未決論点）
  ②★会議室に「判断待ち」のタブが無い（あるのは「承認」だけ）
  ③押せない（build_pending_human は表示専用）
```

**★設計（有璽氏の言葉どおりに読む）**
```
承認        ボタンで即決まるもの      ← ask_hub（既存）
判断待ち    ★承認を含む、もっと広い   ← ★新しいタブ
            ・押せば決まるもの（＝承認）
            ・押すだけでは決まらないが★有璽氏が決めないと進まないもの
              （意匠の判断・正本の削除・本番の操作・方針の分岐）
            ・案件をまたぐ（スタンドアップ／台帳／会議室…）
★1件ごとに「どの案件か」を持たせる。★案件で絞れないと、別案件へ移した瞬間に迷子になる
```

**⛔こちらの整理が古かった（同じ回に訂正された）**
```
こちらが書いていた   スタンドアップの判断待ち4件＝「急がない」
★有璽氏             「スタンドアップは今続けてるよ。やり取りしてますよ」
                    ＝★止まっていない。★急がないは★9/9時点の話で、いまの状態ではない
                    → [[reference_stale_premise_daily]]（土台にも日付がある）
台帳                 ★別案件として進める（有璽氏の決定・2026-09-13）
```

**★「急がない」を理由に一覧から外さない。**外した瞬間に、いま動いているかどうかが見えなくなる。
**★状態（急ぐ／急がない／別案件へ移した）で持つ。消さずに状態で表す**（fukuchi-core）。

## 🔴2026-09-13 会議室と羅針盤で「誰が動いているか」を★別々に計算している（二重管理）

> 有璽氏「同じ概念で作っているはずの、**この ふくちの羅針盤 の方と、この会議室**とかね、
>  **動きがずれてたら、それはそれでおかしいよね。**そのあたり、ちゃんと動作できてんの？」

**★指摘のとおり。同じ問い（誰が走っているか）を、2つの別コードが別々に出している。**

```
会議室   office_data.py build_running()
         ps（claude -p）＋ run_agent_launch.log でPID照合
         🔴★ps のオプションが macOS に無く、★常に0件を返していた（下記・真因）

羅針盤   dashboard_projects.py agent_activity
         run_agent_launch.log ＋ ps ＋ stall_watch_state.json の3点
         14体の名簿に running / stalled_suspect を立てる形

羅針盤   dashboard_data.py agents（14件）
         ★静的な名簿。last_active は★全員 null ＝ 生きた情報ではない
         ＝ ★同じ画面の中にも「動いている風の欄」が2種類ある
```

**★正本が決まっていない。**fukuchi-core「何かを作る前にどれが正本か先に決める」に反している。
**★直し方は1つ ── 走行判定を1本の部品へ寄せ、会議室も羅針盤もそれを読むだけにする。**
数え方が2つある限り、いつか必ず食い違う → [[feedback_never_write_an_unmeasured_number]]

## 🔴2026-09-13 「担当の動き出しは即時」は★実装されていなかった

```
有璽氏の決定（9/12）  画面の更新30秒／★担当の動き出しは即時／レジスタ30分
実測                  run_agent.sh に office_realtime_push の呼び出し ★0件
                      ＝ 起動した瞬間にBlobへ push する経路が★無い
                      ＝ 実際の反映は cron（3,33分）の★最大30分遅れ
```

**★「30秒ポーリング」は画面が取りに行く間隔。★中の数字が新しくなる間隔とは別。**
30秒で取りに行っても、取りに行く先が30分前のままなら、画面は30分古い。
**★2つの間隔を混ぜて「30秒で更新」と言わない。**

## ⛔2026-09-13 リリスは「動いている」ではなく「16分で自分から終えていた」

```
10:50:08  起動（run_agent.sh 経由・PID 68610）
11:06:24  ★完了 rc=0（16分）
成果物    sweep_widths.py 12,232バイト ★在る
          ★出口の sweep_result.md ★無い ＝ 測っていない
ログ      ★3行だけ。最後は「そのまま終えます」＝★自分から途中でやめた
```

**★[[reference_offload_long_work_to_mini]] の「意図と違う動作で正常終了する」型の再発。**
**★rc=0 なので、どの見張りにも掛からない。**有璽氏が画面を見て気づくまで誰も知らなかった。
★見張りは「止まった」を見ている。**「早すぎる正常終了」を見ていない。**

## ✅2026-09-13 外部調査の結論（クローバー・出典つき449行）── ★設計の判定が出た

正本 `~/.vivid-relay/agent_console_research.md`（★MacBook側・43KB）。★実物を読んで確認した。

### ★結論1 ── `claude -p` を毎回起動→終了する作りは★変えなくてよい

```
外の実行基盤は4系統
  ①サブプロセス方式   ★Anthropic公式 Claude Agent SDK 自身がこれ ＝★うちと同じ
  ②常駐ランタイム     AWS Bedrock AgentCore
  ③durable execution  Temporal / LangGraph（落ちても続きから再開）
  ④ジョブキュー        bounded worker pool
```
**★うちの①は標準的。**Temporal/AgentCore級への移行は**14体の社内ツールには過剰**。

### 🔴結論2 ── `ps` で外から数える設計は★妥当でない（数え方の修正では直らない）

```
状態を誰が持つかは3パターン
  1 実行基盤が持つ    AgentCore/Temporal。正確だが基盤の刷新が要る
  2 ★外から観測して推測  ＝★うちの現状。業界が繰り返し「不十分」と指摘する型
  3 ★エージェント自身が申告（heartbeat push）
    ★基盤を変えずに導入できる。★外が実際に使っている最小の是正策
```

### ★結論3 ── 「サイレント障害」は★業界共通で、Temporalを使っても防げない

> 「The process keeps running. The heartbeat continues. But work stops happening.」
> （プロセスは動き続ける。心拍も続く。だが仕事は止まっている）

AIエージェントは**スタックトレースを吐いて落ちない**。ループにハマる・レート制限で
無期限に待つ・単に推論が止まる、という形で**静かに失敗する**。
★複数の独立した情報源で一致（dev.to 3本・Latitude・Telerik・arXiv ResearchGym）。
★ResearchGym の実例：**非同期ジョブを55回超・10分超モニタリングし続けたが、
訓練が止まっていることを検知できなかった。**

**★これはうちだけの未熟さではない。**そして**★設計を全面刷新しても今日の事故は消えない**
（心拍を書く主体が要る、の1点は基盤を変えても変えなくても共通で要る）。
→ うちの「リリスが16分でrc=0終了・出口ファイル無し」は、まさにこの型。

### ★最小の是正（外がやっていること）

```
run_agent.sh が起動時に1行書いて終わりにするのをやめ、
★担当が生きている間、一定間隔で「まだ生きている・いま◯◯をしている」を書き続ける
  → ~/.vivid-relay/agent_heartbeat/<agent>.json へ {updated_at, doing} を上書き
画面は「最後の更新から何分経ったか」で判定する
★ps は補助として残してよいが、★単独の判定根拠にしない
  （規範「1経路で断定するな」と外部知見が一致した）
```

### ★画面の項目 ── 外の定番と、うちの過不足

```
外の定番（複数の独立事例で確認）
  状態3〜4区分／直近の実行内容／★トレース（何を入出力したか）／
  レイテンシ・トークン・コスト／承認待ちキュー／★成功率・健全性の集計
★うちに無い   トレース（今どのステップか）・成功率/健全性・承認待ちの専用一覧
★うちだけ     ★複数のAI担当を「オフィス」として並べる事例は★0件
              （11x/Paradox/Sierraは対顧客AI1体の擬人化どまり）
              ＝有璽氏の「会議室」は★先行の公開事例が無い独自路線。削って標準に寄せない
★進捗％       ★外にも出している事例が見当たらない＝★作らなくてよい根拠になった
```

### ★クローバーが「分からなかった」と正直に書いた点

- 「会議室・オフィスの絵」ずばりの事例0件は**網羅的に探せていない**（WebSearch数回のみ）
- 国内「AI社員」プロダクト（teammates.ai等）の画面詳細は未確認
- HumanLayer の承認インボックスの列構成／Salesforce Agentforce の画面項目は取得できず
- heartbeat の**実運用の間隔・タイムアウトの具体値**は一般論どまり

**★この4点は「まだ調べていない」であって「無い」ではない。**次に深掘りするならここ。


## 🔴2026-09-13 ②「なんでこんな崩れやすいの？」── 真因は席の増加ではなかった

有璽氏「そもそもなんか崩れてるしな、これ今見てるのだけでも。なんでこんな崩れやすいの？」

**★席を14→15人にしたせいだと疑ったが、違った。**実測（ヘッドレスChrome・11幅）：

```
幅        1440  1280  1100  1000   950   900   820   768   640
横あふれ     0     0     0     0     0    46   129   176   304 px
```

**真因は1行。**

```css
@media (max-width:980px){ .cols{grid-template-columns:1fr} }
                                                   ↑
  ★1fr は minmax(auto,1fr)＝min-width:auto。グリッドの列は中身より小さくなれない。
    980px以下で1カラムにした瞬間、列が 930px のまま固まり、画面からはみ出した。
```

**直し方も1行。** `minmax(0,1fr)` ＋ `.cols > *{min-width:0}`
→ 980/900/768/640/500 の5幅すべて横あふれ0を実測。

★この型は LIFE STAND UP で何度も踏んだ「親は縮むが子が追従しない」と同じ根。
**grid/flex の子には `min-width:0` を書く**が既定になる。→ [[project_web_build_rules_asset]]

## 🔴2026-09-13 ③「終わるまでの実績」にテスト用の起動が混ざっていた

`hbprobe 0.9分 / hbprobe 1.1分 / tsuru_probe 0.1分 / lilith_probe 0.2分` が実績に入り、
中央値を押し下げていた。**除いた（消していない・数えないだけ）。**
除外後の実測 ＝ 6件・中央値16.8分・最短9.3分・最長35.9分（見本は「中央値28分」だった）。

★`run_agent.sh` はプローブも本番も同じログへ書く。**数える側で分ける。**

## ★2026-09-13 直したもの一覧（全部この日）

```
走り出した／経過／終わる見込み／最後に動いた   心拍の started_at と実績の中央値から
次の見張り／次の報告／次に動くもの6行          ★crontab と daily_jobs.conf の実物から
中央値・最短・最長・件数                        run_agent_launch.log（プローブ除外）から
担当◯人／今日◯席／ヘッダーの人数              名鑑（.claude/agents/*.md）から
横あふれ                                        minmax(0,1fr)+min-width:0
```

★`running_pinged_at` が None に戻るのは仕様どおり（`office_realtime_push.py` が
30分ごとに全体を書き直すため）。**画面には出ていない診断用の欄なので実害なし。**


## ✅2026-09-13 ④ 作り物の混入を毎日数える検問を入れた

有璽氏「**ちゃんと動き続けるものなの。ちゃんとそれを見といてや。**」への答え。

```
実体      ~/.vivid-relay/office_fake_check.py
cron      50 8 * * *（毎日08:50）→ office_fake_check.log
心拍      ⚙️自動処理レジスタ「会議室に作り物の値が混ざっていないか」
          ページ 3da7b156-8b57-8151-a598-d6d310a8c9f6
          ★有効=False のまま。★明朝08:50の自然発火を1回見てから True へ
失敗時    findings_tracker へ積む（3日続けば既存の経路でSlackへ上がる）
壊して確認 est-start の id を外して 16:54 を埋める → 1件検出・rc=1
```

**★この検問が入った直後に、自分でさらに6件見つけた**（実績バーの作り物）。器に戻した。

## ✅2026-09-13 ⑤ 走行中0人のときの表示

`agent_running.last_finished()` を新設し、`running()` の戻りへ `last` を載せた。
走行中が0人でも「最後に走った担当／かかった時間／終わった時刻／それから」を出す。
**★見出しの語も状態で切り替える**（走行中は「終わる見込み」、終了後は「終わった時刻」）。

🔴**踏んだ罠**：`running()` には return が2つある（心拍経路とpsの予備経路）。
正規表現で最初の1つに入れたら**予備経路のほうだった**＝`last` が常に null。
★**同じ関数に return が複数あるときは、どの経路が実際に走るかを実測してから入れる。**


## 🔴2026-09-15 本当に止まった ── Vercel Blob が Billing State: Inactive で停止

有璽氏「なぜかわからないが止まってるよ。なんで？」

```
実測  blob list-stores --scope fuku-chi-vivid
        fukuchi-kadoban-data      ● Suspended  206.32KB  202ファイル  7d
        lifestandup-editor-data   ● Suspended  411B      2ファイル    5d
      get-store → Billing State: Inactive ／ Updated At: 2026-09-15 00:42:01
```

**★容量超過ではない。課金が有効でないため。** 判断は ask_hub `#3c17b2` で有璽氏へ
（1 課金を戻す／2 Blobを使わない形へ作り替える（費用0・更新は30分間隔になる）／3 止めたまま）。
押す場所 https://vercel.com/fuku-chi-vivid/~/stores/blob/store_q0z2tSgdx1CBz2qc

**★9時間気づけなかった理由は別にある** → [[reference_two_silences_hide_a_stop]]
①cronの出力がどこにも残らない書き方だった ②画面が「読めていない」と言わなかった。
どちらも直した（可逆なので判断を待たずに実施）。

## 🔴2026-09-15 走っているのに0人と出ていた（これも同じ日に見つかった）

```
実測  ps      リリス(69923)・ピタゴラス(69925) が走行中
      心拍    9/15 のファイルが★1つも無い（run_agent.sh を通さない直接起動）
      running() → count=0 ／ error="psにだけ居る(心拍を書いていない起動)"
      画面    ★その error を1行も出していなかった
```

**★心拍だけを見ると、直接起動は永遠に0人。** ps で見えているなら走っている。
→ `agent_running.py` を直し、**心拍を書かない起動も件数・一覧へ入れる**ようにした。
担当名は**指示文の「あなたはリリス（web-developer）です」から読む**（推測ではない）。
経過は ps の etime から。**doing だけは心拍にしか無いので「分かりません」と正直に出す。**

🔴**踏んだ罠**：`noteEl` が既に使われており変数名が衝突。`node --check` が捕まえた。
**★出していたら画面が丸ごと止まっていた**（本物の「止まってる」を自分で作るところだった）。
★JSを触ったら毎回 `node --check` を通す。
