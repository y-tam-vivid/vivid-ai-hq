---
name: reference_claude_design_local_edit_not_reflected
description: Claude Design の .dc.html をローカルで書き換えても画面に反映されるとは限らない。image-slot は別ファイル(.image-slots.state.json)に画像を持つ。書き出し形式は3パターンあり、DesignSyncツールはdesign-system専用
metadata:
  type: reference
---

**★`.dc.html` の `src` を書き換えても、Claude Design の画面がそう表示するとは限らない。**

2026-09-09（作業は9/8）、かわちばなしの画像18枠を差し替えたあと、有璽氏
**「Claude Design の方を見てるんですけど、なんかね、写真が差し替わってないんだよね」**。

## こちらが見落としていたもの

```
かわちばなし トップページ v4.dc.html      ★これしか見ていなかった。src を書き換えた
.image-slots.state.json  (119KB・隠しファイル)  ★見ていなかった
  中身 = { "v2-hero": { s, x, y, u } }
        u = data:image/webp;base64,…  ← ★画像そのものを抱えている
        s / x / y = 拡大率と位置（スロット内でのトリミング）
.thumbnail                                 ギャラリー用のサムネイル（これも古いまま）
```

**★隠しファイル（ドット始まり）は `ls` の既定で出ない。** 最初にフォルダを見たときは
`ls -la` で出ていたのに、**「画像は image-slot タグにある」と決めつけて中身を開かなかった。**

## ⛔結論：B で確定（2026-09-09 有璽氏の目視）

有璽氏「**トップ画像だけでなく、全ての画像、インスタの投稿、サムネイル等も含めて全てできていません**」。
state.json にあるのは v2-hero の1枠だけなので、**全部が変わらない＝state.json の問題ではない。**

```
Claude Design（クラウド側）   ★ここが正本
      │  書き出し（★一方向）
      ▼
~/Downloads/<プロジェクト名>/  ★ただのコピー。ここを直しても戻らない
```

**★裏付け**：19時ごろ向こうがフォルダを作り直した（v2/v3/無印の .dc.html・22MBの書き出し版・
控えが消えた）のに、`v4.dc.html` の mtime は **18:03 のまま**＝
**向こうはこのファイルを読みも書きもしていない。**

**★このフォルダを編集対象にしない。** 読む（構造を知る・静的化して見せる）のは有効。
**書いても無駄で、しかも「直った」と誤認する。**

## 型として

- **★「ファイルを書き換えた」は「画面が変わった」ではない。**
  相手のツールが持つ状態ファイルを見ないまま「差し替え完了」と報告した。
  → [[feedback_write_for_the_reader]]「反映した と書くな。どこで見えるかを1行で言う」
- **★実測は全部こちら側で閉じていた**（cdn参照0件／uploads参照12種／Artifact側21枚）。
  **どれも「ファイルの中身」の実測で、「画面の表示」を1度も見ていない。**
  ★人のツールの画面が出口なら、そこを見るまで完了と書かない。
- **★フォルダを開いたら隠しファイルの中身も1回開く。** 名前に `state` が入っていれば特に。

## ★2026-09-09 指示文に「前提」を書かず、相手に無駄な仕事をさせた

18枚は**もう手元にできていた**のに、Claude Design へ渡した指示文には
「下の指定で全部作り直してください」＋プロンプト18本しか書かなかった。
**向こうは自分で生成しようとして、Gamma のクレジット切れ（402）で止まった。**

```
書くべきだった   ★18枚は既にある。生成は不要。★各スロットへ配置してほしい
                 どのファイルがどの枠かの対応表
実際に書いた     「作り直してください」＋生成用プロンプト18本
相手の答え       「クレジットが尽きています。画像を添付いただければ配置します」
                 ★答えは向こうの回答の中にあった
```

- **★依頼文の冒頭は「何をしてほしいか」ではなく「いまどこまで出来ているか」から書く。**
  出来ているものを言わないと、相手は最初からやり直す。
- **★こちらの誤った見立て**：「元の画像が design-anything ＝ Claude Design 自身が作れる」
  と判断した。**作れるが、その先は Gamma のクレジットで、そこが尽きていた。**
  **★「そのツールが作れるか」と「いま作れるか」は別。** 残量を確かめずに経路を選んだ。
- 渡すのは画像そのもの。**ファイル名を「枠id＋画面の言葉」にすると相手が対応づけできる。**

## ✅2026-09-09 解決 ── これが唯一通った経路

有璽氏「クロードデザイン側に無事に反映されました」。

```
✕ 通らない   Downloadsのフォルダの .dc.html を書き換える（18枠やって全部無効）
✕ 通らない   生成プロンプトを渡して向こうに作らせる（Gammaのクレジット切れ402で停止）
○ 通った     ★画像ファイルをチャットに添付 ＋「生成不要・配置だけ」の指示を貼る
```

**★渡し方のコツ（これで一発で通った）**
- ファイル名を `01_v2-hero_トップの大きい写真.png` の形にする。
  **番号（配置順）＋スロットid＋画面の言葉** の3つを入れると、向こうが対応づけできる。
- 指示文の**1行目を「★画像の生成は不要です。18枚とも添付済みです」にする。**
  ここを書かないと、相手は最初から作り直そうとする（実際に1回させた）。
- 「レイアウト・配色・書体・文言には手を入れない。変えるのは写真だけ」を明記する。
- 置き場 `~/kb_images/配置用/`（18枚・38.8MB）。Finder で開いて ⌘A → ドラッグ。

**★次に Claude Design の画像を差し替えるときは、この節だけ読めば足りる。**

## ★役割の確定（2026-09-09 有璽氏）

**「作るのそのものは、Claude Design 側で作ってもらうので、その指示を出せるような状況にしたい」**

```
こちら（Claude Code）    ①実物を読んで診断する ②Claude Design へ渡す★指示文を作る
                        ③返ってきたものを検査する ④社外へ出す形（PNG＋Drive）を作る
Claude Design           ★作る側。デザインの実装はここ
```

**★こちらでデザインを作り込まない。** 2026-09-08 はローカルの `.dc.html` を18枠書き換えて
全部無効になった。**作るのは向こう、こちらは指示と検査。**
指示文の書き方（前提を1行目に書く・ファイル名に枠idを入れる）はこのファイルの上の節にある。

## ★2026-09-09 分かった制約3つ（Claude Design 側の回答）

```
① ★Google Drive のリンクを開けない
   こちらは指示文をDriveへ上げてリンクで渡していたが、向こうは開けない。
   ★通っていたのは有璽氏が本文をコピペしていたから。
   → 指示文は「そのまま貼れる本文」で渡す。リンクは有璽氏が読む用であって、
     Claude Design へ渡す経路ではない。

② ★ロゴPNG1枚から絵の要素は切り出せない
   「周囲のクリーム地・隣の絵の一部が混ざり、拡大でぼやける」（向こうの回答）。
   ★こちらの指示「ロゴの中の 寺の門・ぶどう・石橋 が使えます」は無理を言っていた。
   → 元がベクター（SVG/AI）でなければ、パーツ流用は最初から選択肢に入れない。

③ ★画像生成のクレジットは「向こうの財布」
   Claude Design の画像生成は Gamma 経由で、そこが 402（残0）。
   ★こちらの OpenAI API（残あり）とは別の財布。
   → ★絵が要る作業は、こちらで作って添付する方が速い。向こうに作らせない。
```

**★CSSで再現できるものは、絵を作らずに済む。** 今回の実例：
虹色の波線（ロゴ外周の色をCSSグラデーションで）／3つの丸（ロゴの配色・水彩風の質感をCSSで）
／4市のピル ── **この3つはクレジット0で実施できると向こうが回答した。**
絵が本当に要るのは「6つのアイコン」だけだった。**★「絵が要る」と「CSSで足りる」を先に分ける。**

## ★こちらから Claude Design へ送る口は無い（2026-09-09 実測）

「貼ってください」と人に頼む前に探した結果。**次に同じことを聞かれたら探し直さなくてよい。**

```
① memory を検索   Claude Design に触れたファイル10本 → ★「投稿・送信できる」記録 0行
② ツールを検索     Slack送信 / Gmail送信 / Notionセッションへの送信 はある
                  → ★Claude Design へ送るツールは無い
```

**＝渡す口は有璽氏の画面にしかない。** こちらができるのは
①指示文を「そのまま貼れる本文」で用意する ②画像を Finder で選べる形に並べる、まで。
**★人に手を動かしてもらうのは正しい。探さずに頼むのだけが問題。**

## ★静的化で写真が崩れる（2026-09-09・有璽氏が発見）

有璽氏「写真とかも、なんかすごい変な形になっちゃってるよ。わざとではないよね」。
**★Claude Design 側は正しい。崩れたのはこちらの静的化。**

```
症状①  カードの写真が★枠の左上に小さく貼られ、残りが市の色のベタ地になる
真因    ★Claude Design のランタイム（image-slot.js）が画像の大きさを制御していた。
        静的化するとそれが無くなり、img が素の大きさで表示される
直し方  ★個々のimgに手を入れず、CSSを1本足す（既存のスタイルを壊さない）
          [style*='aspect-ratio'] img { width:100%!important; height:100%!important;
                                        object-fit:cover; display:block }
          [style*='overflow: hidden']>img { 同上 }

症状②  アイコン6枚が★壊れた画像のマークになる
真因    src が★URLエンコードされていた（uploads/%E3%82%A2%E3%82%A4%E3%82%B3%E3%83%B31_…）
        base/p で探しても見つからない
直し方  ★urllib.parse.unquote(p) してから探す

症状③  一覧・詳細の画像が0枚になった（mini で作り直したとき）
真因    ★uploads/ しか mini へ送っていなかった。画像は assets/ にもある
        → ★送る前に「そのフォルダに何が要るか」を数える。ls して .html 以外を全部見る
```

**★ビルドの中に自分で検査を入れておいたのが効いた。**
「残る相対パス」「残る .dc.html リンク」「画像の枚数」をページごとに出し、
**不合格が0でなければ画面に出す**形にしていたので、③はデプロイ直後に自分で気づけた。


## 🔴2026-09-10 ★2回目：検査の道具が壊れていて、正しい成果物を「不良」と読みかけた

特集記事Bの修正版を静的化して見たら、**章の絵が左端に小さく貼られていた**。
有璽氏へ「絵が小さく見えます」と報告した。**★向こうの実装は正しかった。**

```
真因   extract_vals.js の React スタブが style を★オブジェクトのまま文字列化していた
       createElement(tag, props) の props.style = {display:'block', width:'100%'}
       → style="[object Object]" ＝★スタイルが全部消える
実測   静的化した HTML に「[object Object]」が★25箇所
向こう fullImg() は width:100%/height:auto、sideImg() は本文の隣に大きく置く実装
       ＝ 指示どおり正しく作られていた
直し   スタブで style がオブジェクトなら CSS文字列へ組み直す（キーはケバブへ）
       修正後 [object Object] は 0 件。絵は全幅で大きく、数字は絵の上に重なって出た
```

- **★「実物を見る」と言っても、見る道具が正しいとは限らない。**
  9/9 は画像サイズ、9/10 は style 全体。**どちらもこちらの静的化の不具合。**
- **★言い切る前に留保したのが効いた。** 「私の静的化の不具合の疑いが濃い」と書いてから
  構造を読み、`[object Object]` を数えて確定させた。
  **★1回目（9/9）は同じ型を踏んだ記録が既にあった。それを思い出せたから留保できた。**
- **★見分け方**：画面がおかしいと思ったら、**まず出力HTMLの中を1回 grep する。**
  `[object Object]` `undefined` `NaN` `{{` が残っていれば、こちらの不具合。
  ★これを定型の検査にする（静的化のたびに数える）。
- **★向こうの構造が進むほど、こちらの道具は遅れる。**
  同じ日に2つ踏んだ ── `window.KAWACHI`（taxonomy集約）と style オブジェクト。
  → [[feedback_categories_always_grow]]「指示した側に残る仕事がある」


## 🔴2026-09-10 同じ日に★3例目：`sc-if` を実装していなかった

特集一覧に「**まだ記事がありません。準備しています。**」が出ていた。記事は6本ある。
**★値は正しかった**（`isEmpty: False` を JSON で実測）。
`render_dc.py` が `<sc-if>` を処理せず、**条件に関わらず中身を出していた**だけ。

```
9/9  ①画像の大きさ      ランタイムが制御 → CSSを1本足して解決
9/10 ②style オブジェクト  React スタブが [object Object] 化 → CSS文字列へ組み直し
9/10 ③sc-if 未実装       条件ブロックが常に出る → expand_if() を新設
```

**★静的化するたびに、この4つを数える**（定型の検査にする）。
```
[object Object]   … style などがオブジェクトのまま出ている
undefined / NaN   … 値の取り違え
{{                … 置換されていない
★出ないはずの文言 … 「まだ〜ありません」「準備しています」など sc-if の中身
```
**★4つとも0でなければ、画面の見た目を根拠に相手へ差し戻さない。**

**★型として**：`.dc.html` は独自タグの集合で、こちらの再現器は**常に後追い**。
**「実物を見た」は「正しく見た」ではない。** 見る前に、道具が今日の構造に追いついているかを数える。


## 🔴2026-09-10 ★最悪の型：検査が「0」を出し、それを「成功」と読んで公開した

有璽氏「**リンクとか動かない。ボタン操作も動いてない。絞る機能が動かない。
もっと見るを押しても反映されない。詳細も全然飛ばない**」。

```
私の判定    「x-dc 0件・sc-for 0件・{{ 0件 → ★未処理0 → 描画された」
実際        ★これらは全部 <script type="text/x-dc"> の中にある。DOMには元から出ない
            ＝★0は「処理された」ではなく「最初から数える対象がそこに無い」
真実        <div id="dc-root"></div> が★空のまま。ページは実質まっさら
```

**★正しい判定は1つだけ：`#dc-root` の中身が空でないこと。**
```python
m = re.search(r'<div id="dc-root">(.*?)</div>\s*<script', dom, re.S)
inner = len(m.group(1)) if m else 0     # ★2000B未満なら描画されていない
```
あわせて `<a>` と `<button>` の個数を見る（リンクとボタンが実在するか）。

**★静的化そのものが誤りだった。** `.dc.html` には**最初から
`<script src="./support.js">` が入っている**（＝dc-runtime。`parseDcDocument`/`sc-for`/
`sc-if`/`renderVals` を持つ公式ランタイム）。**そのまま置けば動く。**
こちらで値を焼き込む必要は無かった。**★焼き込むと onClick の受け手が消え、
絞り込み・もっと見る・状態の変化が全部死ぬ。**

**★動かすための条件（実測で確定）**
```
React / ReactDOM を ★support.js より前 に置く（support.js は window.React を要求する）
  ✕ helmet の末尾に足す → window.React が無く、描画されない
  ○ <script src="./support.js"> の★直前に差し込む
support.js・kawachi-taxonomy.js・image-slot.js は★元から入っている。足すと二重になる
一緒に置くもの：_ds/（CSS）・assets/・uploads/
```

**★同じ日に4回、自分の数え方で0を作った。**
①本文タグだけ数えて写真0 ②クォート限定で h1 を見落とし ③x-dc 0件を成功と誤読
④Chrome連続起動でDOMが取れず「リンク0件」。
**★0を見たら、まず「その0は測れているか」を疑う。** 特に**0を成功の根拠にするとき**は、
**わざと壊した版で同じ検査が「不合格」を出すことを1回確かめる**（正常しか出せない検査にしない）。

---

## ★2026-09-10 ── ★C（編集ツール）との連携の可否を調べた

**★結論：★Claude Design → C は「★半自動」。★C → Claude Design は★ほぼ不可。**

```
Claude Design → C   △ ★起点（有璽氏が画面でボタンを押す）は★人にしかできない
                       その後の変換は自動化できる
C → Claude Design   ✕ ★書き戻す公式の口が、個別サイトのデザインには無い
```

**🔴★最重要の発見 ── ★「Claude Designの書き出し」は★1つの形式ではない。★実績で3種類あった。**

```
a) bundler形式        <script type="__bundler/manifest"> ＋ template(JSONエスケープされたHTML)
   （福地サイト等）     ★フォントがbase64で十数MB。★JSで展開しないと中身が読めない
b) ★生HTML+CSS        ★そのままブラウザで描画できる
   （LIFE STAND UP）   実測：20本すべて `__bundler` が0件・ブラウザで完全描画を確認
c) .dc.html+support.js ★独自タグ(x-dc/sc-for/sc-if)＋Reactランタイム込みの「アプリ」
   （かわちばなし）     ★絞り込み・もっと見る等★動的な機能を持つページはこれ
                      ★静的化（値を焼き込む）すると★動的機能が壊れる（実績あり）
```

**★だから「Claude Designから受け取る」処理は、★まず形式を判別する工程が要る。**
★同じ「書き出し」でも中身が違う。★1つの変換だけ書くと、別案件で必ず壊れる。

**★あわせて分かったこと**
```
★公式のエクスポート  ZIP / PDF / PPTX / スタンドアロンHTML
★Claude Codeへの受け渡し  「Send to Claude Code Web」「Send to local coding agent」
★`claude-design` という★公式MCPサーバーが存在する（claude mcp add で接続できる）
★★このハーネス自体に `DesignSync` ツールが標準搭載されている（★下の節で検証した）
```

---

## 2026-09-10 ★書き出し形式は3パターンある。1つに決め打ちしない

有璽氏「クロードデザインとの自動連携も可能なの？」を受けた調査（実装はしていない）。

```
a) bundler形式      <script type="__bundler/manifest">(フォントbase64)＋
   （福地サイト等）   __bundler/template(JSONエスケープHTML)。JS展開しないと読めない
                     → [[project_html_to_figma_pipeline]] に変換手順あり
b) 生HTML+CSS形式   そのままブラウザで描画できる。LIFE STAND UP v2.1(20本)はこれ
   （LIFE STAND UP） （grep -c '__bundler' が20本すべて0で確認済み）→ この節と同じ話
c) .dc.html+support.js形式  独自タグ(x-dc/sc-for/sc-if)＋Reactランタイム込み「アプリ」。
   （かわちばなし）   動的機能(絞り込み等)を持つページはこれ。静的化すると壊れる（この節の本文）
```

**★どの条件でどの形式になるかは未特定。** 案件の複雑さか、書き出しボタンの種類か、
時期による仕様変更かは分からない。**次に着手する人は、まず `grep -c '__bundler'` と
`grep -c 'support.js'` の2つを数えて、どの形式か判定してから動くこと。**

## ★DesignSyncツール（公式）の存在とdesign-system限定という制約

このハーネスに `DesignSync` という専用ツール（list_projects/get_file/write_files等）が
標準搭載されている。**ただし2点の制約**：

```
① 認証   /design-login が対話セッション必須。非対話(claude -p)・サブエージェントでは
         認証できない（実測：list_projects() → 「headless/SDKでは事前に対話セッションで
         1回ログインしておけば以降は再利用できる」とエラーメッセージに明記）
② 対象   "design-system"タイプのプロジェクトのみ。get_projectの説明に
         「通常プロジェクトへpushしてもdesign systemには変わらない（作成時に型が固定）」
         と明記。★LIFE STAND UP/かわちばなしのような「1サイトぶんのデザイン」は
         通常プロジェクトに当たると見られ、対象外の可能性が高い（★未確認・推測）
```

**＝Cで直した内容をClaude Designへ自動で書き戻す経路は、いまのところ無いと見てよい。**
渡すのは今までどおり人がチャットへ貼る（かわちばなし案件で実際に通った経路・上の節参照）。

**★次に有璽氏が対話セッションで `/design-login` を一度実行すれば**、list_projects/get_file
で実際のプロジェクト種別・構造を読み取り、この推測（design-system限定）を検証できる。
詳細 → `~/.vivid-relay/claude_design_link_result.md`
