---
name: reference_retry_backoff_resets_too_early
description: 再試行の間隔を「繋がった」でリセットすると、繋がった直後に拒否される障害では最短間隔で叩き続ける。★9/2に修正し9/3に検証＝バックオフは正しく伸びたが断絶は9秒→24分に悪化。上限は復旧の遅さの下限でもある
metadata:
  type: reference
---

**2026-09-02 06:29 実測（ロビン・毎朝の棚卸しで発見）。**
Slack Socket Mode の常駐が、**19秒間に9回**続けて接続と切断を繰り返した。

```
06:29:38  disconnect要求: warning              ← Slack側の定期的な張り直し（正常）
06:29:40  WebSocket接続完了 → too_many_websockets
06:29:42  WebSocket接続完了 → too_many_websockets
     …（同じ形で計8回）…
06:29:57  WebSocket接続完了 → 接続確立（hello受信）   ← 9回目でようやく収束
```

**★9行すべてが「1秒後に再接続」。指数バックオフは一度も効いていない。**

## なぜ効かなかったか（`slack_socket.py:664-708` 実測）

```python
url = open_connection()
ws  = WSClient(url)
log('WebSocket接続完了')
backoff = 1            # ★ここでリセットしている
state.connected = True
while True:
    msg = ws.recv()    # ← 実際に使えるかどうかは、この先で分かる
```

**リセットの位置が「ソケットが開いた時点」で、「安定して通信できた時点」ではない。**
Slack は**開かせてから**`too_many_websockets` で切る。したがって毎回
`backoff = 1` を通ってから落ちる ＝ **1秒間隔の無限ループになりうる。**

そして原因は Slack ではなくこちら側 ── **1秒で張り直すので、
Slack から見ると古いソケットがまだ生きている。**
`too_many_websockets` は**自分の速すぎる再接続が自分で作った拒否**。
今回19秒で抜けたのは運で、収束を保証する仕組みは無い。

## 型として（Slackに限らない）

- **★バックオフのリセットは「接続できた」ではなく「一定時間、正常に使えた」で行う。**
  接続直後に拒否される障害は、この1行の位置だけで無限リトライになる。
  目安：最初のメッセージを1件受け取る／`hello` 相当を受ける／N秒生存する、のどれか。
- **★リトライが速いこと自体が障害の原因になりうる。** 相手側が資源（接続数・レート）を
  数えている場合、こちらの善意の即時再試行が上限に当たる。
  **失敗の原因を相手のせいにする前に、自分の間隔を見る。**
- **★「1秒後に再接続」が連続で並んでいたら、バックオフは壊れている。**
  正しく効いていれば 1→2→4→8… と**ログの数字が増える**。
  同じ数字が並ぶログは、それ自体が症状。
- **上限（`min(backoff*2, 300)`）があっても意味がない。** 到達する前にリセットされる。
  上限の有無ではなく**リセット条件**を読むこと。

## 見つけ方

```
grep '秒後に再接続' <log> | tail -20     ★数字が単調増加しているか目で見る
                                        同じ数字が3つ以上並べば黒
```

**★ここは「切れた回数」を数えても分からない。** 切れること自体は正常
（[[reference_slack_tokens_and_socket_mode]]「disconnect は異常ではない」）。
異常なのは**間隔が伸びないこと**。件数ではなく間隔の列を見る。

## ⛔ここから下は旧版 ── 2026-09-02 08:43 に修正済み（残す。消さない）

~~**いま直っていない。**~~ `~/.vivid-relay/slack_socket.py` は git 管理外
（[[reference_fix_where_git_reaches]]）。~~★担当＝ピタゴラス（コード変更）。~~
直すのは `backoff = 1` の1行の位置だけ（`WSClient` 直後から、最初の `recv()` 成功後へ移す）。
検査はステラ（[[cross-check]] 型・作った本人は検査しない）。

## いまの状態（2026-09-02 08:43 修正・ステラ検査「載せてよい（条件つき）」）

**ピタゴラスが `backoff = 1` を `type == 'hello'` の分岐へ移した。** 常駐を再起動
（pid 1332 → 19065）。実測：08:43:57「WebSocket接続完了」→「接続確立（hello受信）」以降、
再接続の記録なし。ステラが独立に2経路（diff／実読、ログ4分差の2回観測）で一致を確認。

```
✅検証済     ★2026-09-03 07:50 に自然発生し、1→2→4→8→16→32→64→128→256→300→300
             の単調増加を実測。**バックオフは設計どおり動いた**（下の節）
⛔要訂正     「深刻度は低い／悪化はしない」と書いたが、**悪化した。**下の節が実測
前提         同一トークンで Socket Mode を張るのは slack_socket.py だけ、が前提。
             ★MacBook 等へ複製すると前提が崩れる（~/.vivid-relay/ は git 管理外）
申し送り     hello 判定が :595（handle_envelope）と :695（呼び出し元）の2箇所にある。
             片方だけ直すと壊れる
```

### ★この記録自体が踏んだ穴 ── 索引を直して本文を直さなかった（つる・同日）

08:40 につるが `INDEX_仕組み.md:42` を「未修正」→「発注済み」へ更新したが、
**本文であるこのファイルの「いま直っていない」節は放置した。** 08:45 の commit `ad1cf85`
の時点で、修正（08:43:56）に**文書が1.5分追い越されていた**。ステラの検査で発見。

- **★索引と本文はセットで直す。** これは `bin/memory_audit.py` が毎日検出している型そのもの
  （本文を直して索引を直さない）の**裏返し**で、機械はこの向きを見ていない。
- **★「担当へ投げた」と書いた行は、担当が終えた瞬間に古くなる。** 投げた時点で書くなら、
  戻ってきた時点で書き換えるところまでが1組。**投げっぱなしの記録は、未修正の記録と同じ顔をする。**
  → [[reference_a_warning_nobody_owns]] 5つ目の型と同根

関連 [[reference_ran_is_not_succeeded]]（down_since は再試行ごとにリセットしない・同じ根）
[[reference_heartbeat_proves_life_not_results]] [[reference_a_warning_nobody_owns]]

---

## ★2026-09-03 初の自然発生 ── 直った。そして**悪化した**（ロビン・毎朝の棚卸し）

修正後はじめて `too_many_websockets` が自然発生した。**バックオフは完璧に動いた。**
そして**断絶は 9秒から 23分41秒へ伸びた。**

```
              切断 →  収束        試行  間隔の列
修正前 9/02 06:29   19秒          9回   1,1,1,1,1,1,1,1,1     ← 壊れていた頃
修正前 9/02 08:36    9秒          5回   1,1,1,1,1             ← 同じ「切断された」起点
修正後 9/03 07:50  23分41秒      12回   1,2,4,8,16,32,64,128,256,300,300,300
                   ↑ 07:50:17 切断 → 08:13:58「接続確立（hello受信）」
```

**★同じ起点（`ConnectionError('切断された')` → `too_many_websockets` 連発）で、
9秒が24分になった。変えたのは backoff のリセット位置だけ。**

### この記録の仮説が、実測で支持されなかった

上で「**too_many_websockets は自分の速すぎる再接続が自分で作った拒否**」と書いた。
**それが正しいなら、間隔を空けた今回は速く収束するはずだった。逆になった。**

- 修正前は1秒間隔で叩き続けて **19秒・9秒で収束している**。
  こちらが煽っていたのなら、この速さで抜けられない。
- より整合する読み： **Slack側の古いソケットが消えるまで待つしかない**。
  こちらの間隔は収束時刻をほとんど決めていない。決めているのは
  **「空いた後、次に叩くまでどれだけ寝ているか」**。
- ★**上限300秒は「叩きすぎない上限」であると同時に「復旧の遅さの下限」。**
  条件が解消しても最大5分気づかない。上限を置くなら、この裏側を必ず一緒に設計する。

**★断定はしない。修正前2件・修正後1件の計3件。** 追加の自然発生をあと数回見るまで
「バックオフのせいで遅くなった」と確定させないこと。ただし
**「悪化はしない」と書いた旧記述は、この1件で既に反証されている。**

### 誰も気づかなかった

24分間、営業のSlackボタンは無反応だった。**プロセスは生きて再接続を回していたので
心拍は緑のまま。** → [[reference_heartbeat_proves_life_not_results]] の実例そのもの。
**再接続中は「生きている」でも「使える」でもない、第三の状態。** ここを見る監視が無い。

### 次にやること（担当＝ピタゴラス／検査＝ステラ）

```
1  上限300秒を下げる（例60秒）か、上限到達後は一定間隔で叩き続ける形にする
   ★どちらも「叩きすぎ」との綱引き。実測を数回ためてから決める
2  jitter を入れる（同時刻に複数が張り直す型への備え）
3  「連続失敗がN分続いたら」を心拍とは別に鳴らす（緑のまま沈む穴を塞ぐ）
```

★**1〜3はまだ誰にも投げていない。** 投げたらこの行を書き換えること
（投げた時点と戻った時点は1組 → 下の「索引を直して本文を直さなかった」節と同根）。

関連 [[reference_verify_outcome_not_mechanism]]
