---
name: feedback_never_write_an_unmeasured_number
description: 数字と日付を「その場で」書かない。数える部品・揃える部品を通した値だけを出す。速さのために確かさを落とすのが真因
metadata:
  type: feedback
---

有璽氏（2026-08-23）── **「数字を計算してないっていうのはよくわからんな。なんでそうなんのか謎。
ならんようにどう設計すんの」**

## なぜ起きるか（正直な真因）

**文章を書く速さで数字も書いている。** 「たぶん257だったはず」と**記憶から埋めている**。
実測を挟むと1手増えるので、そこを飛ばす。
＝ **速さのために確かさを落としている。** 知らないから間違えるのではない。

## 同じ日に出た誤り（全部この形）

| 書いたもの | 真因 |
|---|---|
| 「書く前は257件」 | ★数えていない。実測は263件 |
| 「8/31は日曜」 | ★計算していない。実際は月曜 |
| 「エージェント11体」 | 索引を見た。実体（`agents/*.md`）を数えなかった＝13体 |
| 「二重にならない」 | 日付が `2026-08-02` と `2026/08/18` で混在。揃えず突合して偽陰性 |
| 「26行が確認待ち」 | 「確認が空」だけ数え、隣の「照合は不要」を見なかった＝実際は3行 |
| 「明日渡せない」 | 担当の申告を検証せず通した（実行経路は3つあるのに1つしか見ていない） |

**古い情報を持っていたのではない。その場で数え間違えている。**
共通するのは **一部だけ見て全体を語ったこと**。

## ★設計での答え ── 人の規律に置かない。通る道を1本にする

```
いま   その場で数える／その場で書式を決める  → 回ごと・人ごとにブレる
直す   ★数える・揃える を「部品」に固めて、呼ぶだけにする

  数字   count_*() が返した値しか報告に書かない。手で数えない
  日付   norm_date() を通した値しか、比較・保存・突合に使わない
  件数   「443社」ではなく「443社（会社名がある行）」── 数え方を必ず添える
```

**数え方を添えると、間違っていれば書いた瞬間に自分で気づく。**
曖昧なまま出せなくなるのが効く。

## 守ること

- **★報告に出す数字は、そのターンのツール結果から引く。** 記憶・前の発言から引かない
- **★日付は1つの形に正規化してから扱う。** 台帳には人が手で入れた行と機械が書いた行が
  同居し、書式は必ず混ざる（`2026-08-02` と `2026/08/18` を実測）
- **★担当の申告をそのまま通さない。** 「cron未登録」の申告は、実行経路を1つしか
  見ていなかった（cron／日次ジョブ／GASトリガーの3つある）
- 迷ったら **数える前に「何を1件と数えるのか」を先に言葉にする**

関連 → [[feedback_one_route_is_not_verification]]／[[reference_dangerous_entrypoints]]

## ★有璽氏の設計（2026-08-23）── 毎回数えない。1か所に集約して読むだけにする

> 「各エージェントが最新版の情報や数字を、どこか統一した場所に集約して、
>   それを読むだけにすればいいんじゃないの？ 1から全部数えたら、すごい時間かかるじゃん。
>   **ここを見るっていうものを定めていけばいい。** 全部を集約できないまでも、
>   このフォルダであればこの部分を見るとか、そういうふうな形で決め事として作って」

**「毎回数える」より優れている。** 理由は速さだけではない ──
**別々に数えると、数える人ごとに違う答えが出る**（2026-08-23 実測：
番人は「申告倒れ0件」、実装担当は「7件」。同じ日・同じ対象）。

```
① 集約できるもの      数字を1か所に持つ ＝ ★実装は `dashboard_data.json`（読み口が `facts.py`）
                      ★数え方も一緒に持つ（「443社（会社名がある行）」）
                      ★generated_at を必ず持つ（無いと古さが分からない）
② 集約できないもの    「どこを見るか」を決め事にする
                      台帳 → Skill `sheets-access` ／ Notion → 運用ルール正本
                      営業 → Skill `fukuchi-sales`
③ 読む側              ★規律に頼らない。フックが作業の直前に突きつける
```

**★新しい器を作らない。** 既にある `dashboard_data.json`（稼働盤用に実測を集約したもの）を
「数字の正本」へ格上げする。増やすと二重管理になる
（規範「新しい仕組みの採否は二重管理が増えないかで決める」）。

## ★同じ形の変種 ── 「エラーを見ずに、結果の数字だけ見た」（2026-08-23）

Slackのチャンネル一覧を取り、**「0件」だから権限が無い**と報告した。**誤り。**

```
叩いた   conversations.list?types=public_channel,private_channel
返り     {"ok": false, "error": "missing_scope", "channels": []}
             ↑ ここを見なかった              ↑ ここだけ見た
実測     public だけで叩くと **3件返る**。★権限はあった
影響     有璽氏へ「スコープを3つ追加してください」と過剰な作業を渡しかけた
```

**★API の返りは「数字」ではなく「ok と error」から読む。**
`ok:false` のときの空配列は「0件」ではない。**測れていない**という意味。

- **★空・ゼロ・見つからない を結論にする前に、エラーが出ていないかを見る**
- **★リクエストの条件を欲張らない。** 1回で全部取ろうとして
  （public と private を同時に指定して）、権限のある方まで巻き添えで落とした。
  **取れるものから1つずつ取る**
- 人へ作業を渡す前に、**その作業が本当に要るのか**をもう一度だけ実測する
  → [[feedback_verify_before_declining]]（押す場所まで特定してから渡す）

## ★この設計が効いた最初の実例（2026-08-23・実装当日）

同じ台帳を別々の担当が数え、**違う数字が出た。だが今度は原因が一撃で分かった。**

```
つる        443社   「00_企業マスタで会社名がある行」
ピタゴラス  437社   「社内顧客IDがある行（統合済みは除く）」
差 6件      ★全部「◯◯へ統合済み」の行だった（B-0008 / B-0141 / B-0214 /
                                              B-0222 / B-0310 / B-0374）
```

**どちらも正しい。目的が違うだけ。**

```
443社   全部の行を数えたいとき（履歴として残る統合済みを含む）
437社   いま生きている取引先を数えたいとき   ← 営業に見せるのはこちら
```

**★数え方が添えてあったから、食い違いが「謎」ではなく「差分」になった。**
添えていなければ、また「どちらが正しいのか」で1往復していた
（同じ日の午前に、番人0件 vs 実装担当7件でそれをやっている）。

## 実装（2026-08-23）

```
dashboard_data.json  ★数字の正本。各値を {value, how, as_of} の3点で持つ
facts.py             共通の読み口。`from facts import get; get('ledger.companies')`
                     ★generated_at から24時間超は stale:true を返す（30時間経過で実測）
date_utils.py        norm_date() を1本化。★日付を自前処理していたのは3本
                     （ledger_report / intake_register / watch_external）── 未置換。
                     次に触る人はここへ寄せること
置き場               mini の `~/.vivid-relay/`（git管理外）。MacBookでは手動コピーが要る
```

## ★「共通部品を作った」と「全経路が通るようになった」は別（2026-08-23 検査で判明）

`date_utils.norm_date()` を1本置いた。**書式混在は解消しない。**
書き込む側に古い実装が残っていたため。

```
読む側   date_utils を通す        → 正しく揃う
書く側   intake_register.to_date() → ★未対応の書式でも **生の文字列をそのまま返す**
                                    そのまま初回接触日・作成日・更新日へ書き込む
結果     揃える仕組みを入れた当日から、**数えるそばから混在が増え続ける**
```

**★部品を置いただけでは、既存の経路は何も変わらない。**
grep で「自前で処理している箇所」を数え、**書き込む側から先に置き換える**。
読む側だけ直すと「揃っているように見えて、汚れは増えている」状態になる。

```
実測（grep）  日付を自前処理していたのは3本
  intake_register.py  ★本命。台帳へ書く。未対応書式を捏造せず None で止める形へ
  ledger_report.py    独自の正規表現だが実質同等。危険度は低い
  watch_external.py   外部APIのISO日時のみ。台帳とは別ドメイン。影響なし
```

- **★危険度は「どこを読むか」ではなく「どこへ書くか」で決まる。**
  同じ「未置換3本」でも、書き込む1本だけが汚れを作る
- 部品を入れる作業は、**置く → 書く側を置換 → 読む側を置換** の順。
  逆にすると、直したつもりで汚れ続ける

### ★部品の穴は、使い始めて初めて見つかる（2026-08-23）

書き込む側を `norm_date()` へ置換する作業の中で、**その共通部品自体の穴**が出た。

```
"2026-13-45" / "2026-02-30"   ★桁数は合うが、暦として存在しない
いまの norm_date              → 弾けずに **生の文字列をそのまま返していた**
直し                          datetime.date() で実在チェックを足した
```

**作った直後には見つからなかった。** 置換して実データと壊れた値を通して初めて出た。
→ 「部品を置いた」で作業を終えると、**穴を抱えたまま全経路がそれを使い始める**。

## 予防修正は、効果を直接確認できないことがある（正直に言う）

`to_date` の置換をドライランで通したところ、受付シート28行は**全部同じ書式**で、
**差分0件**だった。

```
確認できたこと    3書式とも正しく揃う／壊れた値は捏造せず止まる（合成データで実測）
確認できないこと  ★実データでは差分が出ない＝修正の効果をこのデータで示せない
```

**「差分0件だった」を「直っていない」と読まない。** 想定外の書式が来た日に効く予防。
ただし **効果を実データで示せていないことは、そのまま申告する。**
（黙って「直しました」とだけ言うと、検証済みだと誤解される）

### ★秘密は「消す」より「出さない」（2026-08-23 実地）

有璽氏が Slack DM へ貼った app-level token が、**そのままログへ平文で残っていた。**

```
残っていた   slack_inbox.log ／ slack_inbox_partial.txt
なぜ         受信の仕組みが **DMの本文をそのままログへ書く** 作りだった
対処①       残っていた2箇所を伏せ字にした（chmod 600）
対処②       ★これだけでは次に貼られたときまた残る
             → redact() を通してからでないと本文をログへ出さない形にした
```

`xapp-` `xoxb-` `xoxp-` `secret_` `ntn_` `sk-` `ghp_` `AIza` `ya29.` を伏せる。
**★普通の文は素通りすることも実測した**（「はい」「明日の商談は10時からです」はそのまま）。
伏せすぎると業務のログが読めなくなる。

- **★人に鍵を渡してもらう経路を作るときは、受け取る側のログを先に見る**
- 鍵を貼ってもらったら **貼った側のメッセージを消してもらう**
  （ボットは自分の発言しか消せない）

### ★配列の位置と列記号を混同した（同日）

```
実測      hd.index('確認') → 24
伝えた    「Y列」
★正しい   0始まりの 24 は **Z列**（Y列は 23 ＝ 照合結果）
```

**0始まりの位置をそのまま列記号に読み替えない。** 変換は必ず関数を通す。
→ この形も「測っていない数字を書く」と同じ。手で数えた瞬間にずれる。

### ★「解消した件数」も測っていない数字だった（2026-08-26 実地）

契約書のレッドライン反映で、**起草した本人が「重大リスク12件のうち11件解消」と申告した。**
別主体で1件ずつ突合したら、**完全に閉じていたのは7件**だった。

```
申告 11件解消        起草者が自分の作業を振り返って数えた値
実測  7件解消        別主体が条文へ当たって突合した値
残り  ①直し漏れ3件（別紙に固定日付が残る／参照先の条だけ直っていない）
      ②過剰修正1件（対策を入れた結果、当社の権利を打ち消した）
```

- **★「直した」は作業の記憶であって、条文の状態ではない。**
  数えているのは「自分が手を動かした回数」で、「実際に閉じたか」ではない。
- **★過剰修正は申告に絶対に出ない。** 直し漏れは本人も気づくことがあるが、
  「入れた対策が別の条を打ち消した」は**入れた本人には成功に見える**。
  だから件数を自己申告させても出てこない。**別主体で読むしか検出手段がない。**
- **対策**：進捗・解消・削減の件数を報告させるときは、**「申告ではなく突合した数で書け」と
  明示的に指示する**。指示しないと作業の記憶が数字として出てくる。
- 検算は別主体で行う。→ Skill `cross-check`／[[feedback_one_route_is_not_verification]]

### ★★再発 ── 「申告をそのまま通さない」と書いた同じセッションで、申告をそのまま流した（2026-08-26）

契約の検査で、起草者の「12件中11件解消」が実測7件だったことを教訓として**このファイルへ追記した。
その30分後、同じ起草者の自己点検の数字を、検算せずに有璽氏へ報告した。**

```
こちらが有璽氏へ書いた   「内部参照85件を1件ずつ突合 → 解決できないもの0件」
実際                     85件は起草者の自己点検の数字。★条見出し24件を参照として数えていた
検査役の実測             前回44件 / 今回60件（見出し24・法令6を除いた真の内部参照）
                         ★「解決できないもの0件」という結論だけは一致していた
```

- **★結論が合っていたことが、いちばん危ない。** 数え方が違うのに結論が同じだと、
  誰も検算しない。次に数え方が結果を変える場面で、初めて破れる。
- **★教訓を書くことと、実行することは別。** 書いた本人が同じターンで踏んだ。
  **記録は再発を防がない。検算の手を動かすことだけが防ぐ。**
- **対策**：担当・下請けの報告に数字が出てきたら、**有璽氏へ渡す前に「その数字は誰がどう数えたか」を
  1行で確かめる**。確かめられないなら「◯◯（担当）の申告では」と出所を付けて渡す。
  **裸の数字として自分の報告に取り込まない。**
## ★真因の一段下 ── 「結論を先に置き、検証がそれを覆せない構造にする」（2026-08-29）

有璽氏「**そういうのをやめろ、クセっつってんねん。直しゃあいいっちゅう話しちゃうやろ。
混ぜましたとか、そういう話しちゃうやろ。そういうのを積み重ねんねやろ。**」

「速さのために確かさを落とす」の下に、**もう一段の構造**がある。
2026-08-29 の1日で、同じ形が4回出た。**全部、結論を先に書いて、検証を後ろに置いている。**

```
① 索引の行長を測るコード
     for ln in ...:
         if b > 180: print('★超過', b)
     print('新規6行はすべて180バイト以内')   ← ★条件と無関係に必ず出る固定文字列
     実際は4行が超過していた。**判定が結論を覆せない構造をコードに書いた**

② 閾値の決定
     実測55%を見てから「50%以上なら🟢」と置いた
     ＝ 緑という結論が先。基準を後から合わせた（正すと空振りが1件→3件に増えた）

③ build_effectiveness()
     全チェックが例外で落ちると out=[] → 「🟢 全部効いている」
     ＝ 何も数えていないのに正常。判定不能を正常へ倒した

④ build_landmine_index.py
     NameError を出しながら exit=0 を返し、古い json を残した
     ＝ 失敗を成功へ倒した
```

**★共通しているのは「判定より先に結論があり、判定が結論を覆せない」という一点。**
「気をつける」で止まらない。**その構造を作れないようにするしかない。**

### 二度と作らないための形

```
禁止   出力に、判定と結びついていない断定文を置く
         print('すべて収まっています')          固定文字列の結論
         return {"health": "🟢"} の既定値        判定できなくても緑
         sys.exit(0) を無条件で                  失敗しても成功
必須   結論はデータから導出する
         print('超過 %d行' % len(ng))            ng が空なら 0行 と出る
         判定不能は 灰 にして異常に数える
         落ちたら exit 1
```

- **★「あとで直す」で済ませない。** 直したこと自体は成果ではない。
  **同じ構造をもう作れない形にしたか**だけが成果。
- **★自己申告の形で終わらせない。** 「混ぜていました」と書くのは報告であって対策ではない。
  有璽氏が求めているのは、**次の回に同じことが起きない構造**。
- ★これは**この記憶に既に書いてある型の再発**である（「速さのために確かさを落とす」）。
  書いてあっても止まらなかった ＝ **規律では止まらない**。機械で止める
  （[[reference_norms_outnumber_their_enforcement]]）。
### ★対策案そのものが的外れだった（2026-08-29・ピタゴラス実測）

私は「発話の数字が、そのターンのツール出力に実在するか突合する」検査を提案した。
**実測の結果、この検査では今日の失敗①を捕まえられない。**

```
失敗①の実物
  if b > 180: print('★超過', b)              ← ループで正しく判定している
  print('新規6行はすべて180バイト以内')       ← 判定と無関係な固定文字列

実際の出力（両方出ていた）
  ★超過 181 / ★超過 185 / ★超過 213 / ★超過 184
  新規6行はすべて180バイト以内

★「180」も「6」も、ツール出力に実在する。だから実在性の検査では素通りする
```

**★数字は本物だった。間違っていたのは断定のほう。**
つまり要るのは「実在性の検査」ではなく「**網羅性の検査**」── 別のロジック。

### ★そして、いちばん深い失敗はここ

**出力には超過4件がちゃんと出ていた。** 私はその下の固定文字列を報告に採用した。
＝ **矛盾する2つの出力を見て、都合のいい方を採った。**

「測っていない数字を書いた」のではない。**測った結果が目の前にあったのに、
自分が先に書いた結論のほうを信じた。** これが「結論を先に置く」の最終形。

- **★判定を出力する行は、1箇所だけにする。** 出力が2つあると「どちらを採るか」の
  判断が入り、そこで自分に都合のいい方を採る。**選べる状態を作らない。**
- 実測で分かった検出可能性（ピタゴラス）：
  - **捕まえられる** ── 判定語入りの print が、for/if の**外**にある（AST で検出可能）
  - **捕まえられない** ── Bash の echo（シェルは ast で解析できない）／
    数字は実在するが断定が事実と食い違う型（＝失敗①そのもの）は実在性検査では不可
### ★同じログを3者が数えて、3つとも違う数が出た（2026-08-29 実測）

`~/.vivid-relay/role_guard.log` の行数を、同じ日に3者が別々に数えた。

```
検査役ステラ   「5件」      検査した時点では5行だった（正しい）
実装ピタゴラス 「6行」      自分の分を数え直した。★7行目を見落とした
窓口ビビ       「5行」      cat した時点では5行。★その後に自分でテストして6行目を足した
実物            7行         ★誰の申告とも一致しない
```

**★7行目だけ日付が前日（2026-08-28T22:53:33）だった。** 記録側が UTC で書いており、
他の行（JST）と混在していた。**時系列でソートすると最後に来ない**ため、
全員が「最後の行」を見落とす構造になっていた。

- **★悪意も怠慢も無い。全員が正しく数え、全員が違う答えを出した。**
  見た時刻が違い、書式が揃っていなければ、同じ対象でも数は一致しない。
- **★これが「1か所へ集約して読むだけにする」の理由**（有璽氏の設計）。
  数える行為を各自がやると、人数分の答えが出る。
- **★ログの時刻は必ず1つのタイムゾーンで書く。** 混在すると、
  数えられないだけでなく**順序が壊れて最後の行が消える**。
  日付が変わる時間帯に書かれた行は、前日として先頭へ回る。
- 3者とも「実物を見て数えた」と言える状態だった。**それでも一致しなかった。**
  だから「数えました」は根拠にならない。**いつ・どの範囲を数えたかまで書く。**

