---
name: project_ask_hub_push_decisions
description: 有璽氏とAIのやり取りを「押すだけ」にする判断ハブ。営業のAI確認依頼と同じプッシュ型をDMで。2026-09-03 稼働・実地で2件通した
metadata:
  node_type: memory
  type: project
---

# 判断ハブ ── Slackのボタンで返す（2026-09-03 稼働）

**有璽氏「通知は届くけど、何に私は判断したらいいのかがわからない。営業で使っている
AI確認依頼はプッシュ通知になっているので、私とAI側とのやり取りも同じ設計にできないか。
押すだけでOKになるし、違う場合は記述するような形にしてくれたらいい」**

## 使い方（AI側）

```python
# ★必ず Mac mini から発行する（下の「踏むと痛い」参照）
import ask_hub
ask_hub.ask(
    subject='何についてか（案件名・成果物）',
    question='何を決めるのか（1文）',
    options=[('こうする', 'primary'), ('こうしない', None)],   # ★1〜4個
    detail='判断の材料',
    link='押せるリンク',           # 無ければ空。★意味の無いリンクは付けない
    asked_by='ピタゴラス', kind='開発')
```

`ask_hub.py --list` で一覧、`--show <受付番号>` で1件、`--preview`（引数なし）で投げずに中身を見る。
答えは `ask_hub.answer_of('受付番号')` で読む（未回答なら None）。

## 経路

```
AI ──ask_hub.ask()──▶ 有璽氏のDM(D0AT4NQ6X7D)
                      本文＝案件名／何を決めるか／材料／依頼元・種別・受付番号
                      [選択肢1][選択肢2][✍️ その他（書いて返す）]
  押した ──▶ slack_socket.py の interactive 経路（営業のボタンと同じ常駐）
             → 元メッセージが「✅ 受け取りました ── ◯◯」に変わる（ボタンが消える）
  その他 ──▶ Slackのモーダルが開く → 自由記述 → 同じ interactive 経路で戻る
             ★モーダルの送信も interactive なので、新しいスコープも購読も要らない
  開けない ─▶ 「DMに直接書いて」→ slack_inbox.py（cron */5）が拾う
```

## ★踏むと痛いところ

- **★発行は必ず Mac mini から。** 台帳 `ask_hub_queue.json` は受信側（mini）にしか無い。
  MacBook から投げると、押されたときに「受付番号が見つかりません」になる。
  `ask_hub.py` 自体は両機に在る（MacBook からは投げられるが**投げてはいけない**）。
- **★`notify.py` の `slack_pending.json` とは別物。混ぜない。** あちらは
  「答えが来るまで後続を一切進めない」設計で、実際に14日間パイプラインを塞いだ
  → [[reference_pending_decision_does_not_pause_the_pipeline]]。
  こちらは**誰も止めない**。止めたい処理は自分で `answer_of()` を見に来る。
- **★宛先は `ask_hub.ROUTES` の1か所だけ。** 2026-09-03 有璽氏「営業のやつ以外はDMで」。
  チャンネル分割は**許可であって指示ではない**（基準が未決）。分けるならこの表に足すだけ。
  種別（開発/営業/福祉/…）は1件ずつ記録してあるので、分けるときに数えて基準を決められる。
- **★選択肢は4個まで**（コードで止まる）。5個要るのは、こちらが決め切れていないだけ。
- **★リンクは押して意味のあるものだけ。** 1通目で「Slackのトップ画面」を入れかけたが、
  押しても同じ画面に戻るだけなので外した。

## 実地の記録（2026-09-03）

```
19:59  #2c20d2 を投稿（Slackの再接続の上限を下げてよいか）
       届いたことを2経路で確認：Slack側 conversations.history の実物／こちらの台帳
20:01  ★有璽氏が「入れてよい」を押した。150秒で回答。元メッセージが
       「✅ 受け取りました ── 入れてよい（#2c20d2）」に変わったことを Slack 側で確認
20:02  承認された修正を適用（slack_socket.py の MAX_BACKOFF_SEC 300→30）
20:03  #4a6383 を投稿（8/20から止まったままの pending の解消）
```

**★仕組みは自動処理（cron/daily_jobs）へ登録していない。** ドーベルマンの検査前のため。
**★ステラのコード検査は未了**（作ったのはピタゴラス。自己採点にしない）。

## ★出口が2本あると、翌日には記述式へ戻る（2026-09-04 有璽氏の指摘）

**有璽氏「選択式で依頼していたのに、記述式に変わってるよ」（08:54）。**
ボタンを作った翌朝には、判断依頼が文章に戻っていた。**仕組みを入れても、
古い出口が残っている限り、そちらが使われる。**

```
出口①  ask_hub.ask()   ボタン。押すだけ。★9/3 に3件だけ通った
出口②  notify.ask()    ★本文に「番号だけでOK」と書く記述式。ボタンを出さない
                       （_post() は text だけを送り blocks を使っていない）
出口③  notify.tell()   報告。★ここへ判断を紛れ込ませると、いちばん悪い形になる
```

**実測（2経路）** ── 経路1＝Slack DM の実物 ／ 経路2＝`notify.py` のソース直読・`ask_hub.log`。

```
9/3 15:00  出口② 「1 確認しました／2 いまは保留 … 番号だけでOK」＝記述式
9/3 19:59〜20:14  出口① ボタン3件。有璽氏はいずれもボタンで回答（ask_hub.log）
9/4 08:34  出口③ 棚卸し報告。冒頭に「報告です。返信は要りません」と書きながら、
           末尾で「検査＝規範側の変更なので判断を仰ぎます」と判断を求めていた
           ＝規範「報告と判断依頼を混ぜない」に正面から反する
```

- **★判断を求めるときは必ず `ask_hub.ask()`。** `notify.ask()` は使わない。
  報告 `tell()` の中に「どちらにしますか」を書かない。**判断は必ず別の1通に切り出す。**
- **★直った基準は部品の言葉で書かない** → [[reference_verify_outcome_not_mechanism]]。
  ここでの基準は **「有璽氏が判断を求められたとき、必ず押せるボタンが付いている。
  番号を打たされることが一度も無い」**。`blocks` の有無や関数名で合格を出さない。
- **★同じ型が別の場所でも起きる。** 新しい経路を足したら、古い経路を**塞ぐ**ところまでが
  ワンセット。残すと必ず戻る → [[reference_stale_premise_daily]]。

## 🔴 全AI・全担当への恒久ルール（2026-09-04 有璽氏・★2回目の指摘で規範化）

**有璽氏「ここで選択、クリックこちらがするような形の選択式にしてほしいって言ったんだけど、
また元に戻ってしまっている。これ、他のエージェント含めて全体に周知して。Slackの通知の仕方、
特に私の判断が要るものに関しては選択できるものにして。」**

```
★判断が要るもの   必ず ask_hub.ask()。ボタンを付ける。番号を打たせない
★報告            notify.tell()。冒頭に「返信は要りません」。★判断語を1つも入れない
★禁止            notify.ask()（記述式）を新しく呼ばない。tell()に判断を紛れ込ませない
```

- **★これは1担当の話ではない。ビビ・ピタゴラス・リリス・つる・ドーベルマン・ステラ・
  ロビン・ナミ・センゴク・フランキー・モルガンズ、全員に効く。**
  自分の担当領域から有璽氏へ何かを送るときは、**その1通が「判断」か「報告」かを先に決める。**
- **★2回言われた＝仕組みで止まっていない。** 1回目（9/4 08:54）は notify.ask() を
  ask_hub へ委譲する改修で直したが、**その改修は同日09:15に巻き戻った**
  → [[reference_two_sessions_built_the_same_thing]]。**直した記録があることと、
  いま直っていることは別。** 送る前に実物（`notify.py` に `ask_hub` があるか）を見る。
- **★「通知が来ない」も同じ回に言われている。** 押せる形にする前に、**まず届くこと。**
  届いていない原因を1経路で「送った」と断定しない → [[feedback_one_route_is_not_verification]]。

## 🔴 押されたことが、聞いた側へ返ってこない（2026-09-04 実地）

**有璽氏「Slackは返答してないかな？」** ── **押してあった。こちらが見ていなかった。**

```
10:58  ビビが #311115 を投稿
11:02  ★有璽氏が「付けない」を押す（4分で回答）
その後 ★こちらは気づかず、別の報告に答え続けていた
       ＝ 有璽氏から見ると「押したのに何も起きない」
```

**★ask_hub は投げっぱなし。** `answer_of()` を**誰かが見に行くまで、押されたことは誰も知らない。**
Slack上では元メッセージが「✅ 受け取りました」に変わるので**押した側は届いた気になる**が、
**聞いた側には何も飛ばない。**

```
いまの逃げ方（規律に依存する）  ★投げたら、次に自分が動く回で必ず answer_of() を見る
本当の直し方（仕組み）          ★回答が入った時点で、聞いた側へ知らせる経路を作る
                                → ピタゴラス領域。まだ作っていない
```

- **★「押すだけ」にしたのは有璽氏の手数を減らすため。** 押しても進まないなら、
  **記述式だった頃より悪い**（記述式なら少なくとも文章が目に入った）。
- **★2件以上を並行で投げているときほど落ちる。** 今回は #ec25ed の答え（10:57）には気づき、
  **その4分後の #311115 は見落とした。**「1回見たから大丈夫」にならない。
- **★有璽氏に催促させた時点で負け。** 待たせている側が確認しに行く。

## 🔴 予約した通知が、時間になって落ちた（2026-09-04）

有璽氏の「今日の15時に再度Slackで通知して」を、mini に `nohup sleep` で予約した。
**プロセスは生きていた。中身が落ちた。**

```
仕込んだとき  ★プロセスの生存を確認して「予約完了」と報告した
15:00        ValueError: 種別は 開発／営業／福祉／広報／財務／法務／個人／その他 のどれか
             （いま '経営'）  ← ★KINDS に無い種別を渡していた
気づいたのは  ★1時間40分後。別件で remind.log を覗いたとき
```

- **★`kind` は自由文字列ではない。** `ask_hub.KINDS` ＝
  **開発／営業／福祉／広報／財務／法務／個人／その他** の8つだけ。
  「経営」「開発部」などは通らない。**渡す前に KINDS を見る。**
- **★プロセスが生きていることは、仕事が成功したことを示さない。**
  → [[reference_ran_is_not_succeeded]]。予約型はここが特に外れやすい
  （仕込みの瞬間は成功して見え、失敗するのは誰も見ていない時刻）。
- **★予約したら、発火時刻の後に必ず結果を確かめる。** 見る場所は
  `~/.vivid-relay/remind.log`（標準エラーの落ち先）と `ask_hub.log`（投稿の記録）の2つ。
- **★仕込む前に1回、同じ引数で `--preview` を通す。** 今回は preview を挟んでいれば
  その場で ValueError が出て、15時を待たずに直せた。**時間のかかる検証ではない。**

### ★同じ日に2回目。今度は通信で落ちた（17:00の分）

```
15:00  ValueError（種別が KINDS に無い）        ← ★引数の誤り。previewで防げた
17:00  urllib.error.URLError: SSL handshake timed out  ← ★通信の一時不調。防げない
```

**★原因は毎回違う。だが結果は同じ ── 予約した通知が届かず、誰も気づかない。**

- **★真因は「1回きりで、失敗を誰も拾わない」構造。** 引数を直しても次は通信で落ちる。
  **潰すべきは個別の原因ではなく、失敗が黙って消える経路。**
  → [[reference_a_warning_nobody_owns]]
- **★入れるべきは再試行。** 送信は数秒で終わる処理なのに、1回失敗して終わっている。
  **3回・間隔を空けて試す**だけで、通信の一時不調はほぼ吸収できる。
  ★これはコード側＝ピタゴラス領域。ビビは仕込む側なので、依頼として出す。
- **★当面の逃げ方**：予約したら**発火時刻の直後に自分で結果を見る**。
  見る場所は `remind.log`（例外の落ち先）と `ask_hub.log`（投稿の記録）。
  **「プロセスが生きている」は結果の確認にならない**（15時の教訓と同じ）。

### ✅ 再試行を入れた（2026-09-04 夜・ピタゴラス）

```
実測  両機の ask_hub.py が sha256 一致（b6eba732…）／再試行の記述21箇所
      ★巻き戻っていない（今日5回巻き戻った notify.py とは別ファイル）
仕様  通信例外（URLError／socket timeout／5xx）のみ最大3回・2秒→4秒
      ★4xx と Slack の ok:false は再試行しない（何度やっても同じ・重複投稿の危険）
      3回とも失敗したら ask_hub.log へ「★送信に失敗した」を1行残す
      docstring の先頭に KINDS（8種）を明記
★未   実際にSlackへ通す通し確認はしていない（テスト投稿はノイズになるので、
      次の本番投稿で確かめる）。ステラの検査も未了
```

**★再試行してよい失敗の見分け方（一般に効く）**

```
◎ 再試行してよい  ★相手に届いていないと言い切れる失敗
                  urlopen() 自体が例外＝レスポンスのヘッダーすら受け取っていない
✕ 再試行しない    ★届いたかどうか分からない失敗
                  urlopen() は成功し、その後の読み取りで落ちた
                  ＝ HTTPの往復は完了しており、相手は処理済みかもしれない
                  → 再試行すると★二重投稿になる
✕ 再試行しない    ★何度やっても同じ失敗（4xx・権限エラー・ok:false）
```
**★「失敗したら3回やり直す」は雑すぎる。**外へ出る処理では、
**やり直すたびに相手側で1回起きているかもしれない**ことを先に考える。

---

## 🔴 端末の承認ダイアログは、Slackの「はい」では解けない（2026-09-05 実地）

**有璽氏が Slack で「はい」と返した。届かなかった。代わりに新しいセッションが1本起きた。**

```
12:36  bot →DM  「承認待ちで止まっています（端末で「はい／いいえ」を押してください）
                  AskUserQuestion」          ← hook_permission_slack.py が出した
12:40  有璽氏     「はい」                    ← Slackへテキストで返信
12:45  slack_inbox.py がそれを新しい依頼として拾い、★ビビを新規起動
       ── 止まっているセッションは、そのまま止まったまま
```

**Why:** `hook_permission_slack.py` の docstring にこう書いてある ──
**「★通知するだけ。承認そのものは端末で行う（フックからは代行しない）」**。
つまりこの通知は**構造的に返信を受け取れない**。にもかかわらず文面は
「押してください」で終わっており、**押せる場所がどの機のどの窓かを書いていない**。
有璽氏から見れば ask_hub のボタンと区別がつかない。

**★ask_hub と承認ダイアログはまったく別物。混ぜない。**

```
ask_hub のボタン        Slackで押す → interactive経路で答えが返る   ◎返る
承認ダイアログの通知     Slackは通知のみ → 押すのは★端末            ✕返らない
```

**How to apply:**

- **この通知が来たら、Slackで答えない。** 動いている機の端末で押す。
  ★どの機かは通知に書かれていない。**通知文へ機名とセッションを入れるのが本来の直し**（未実装）。
- **切り分け方（実測2経路）**：mini の `~/.vivid-relay/.hook_last_notify.json` の
  mtime が通知時刻と一致するか（＝mini発か）／mini の各 transcript に
  その時刻の `AskUserQuestion` があるか。2026-09-05 は 08:40・09-03 が最終で
  **どちらも12:36と合わない → MacBook 側のセッションだと判定した**。
- **ビビへ**：「はい」だけのDMが来たら、まず直前のDM履歴を読む。
  ★承認ダイアログの通知に対する返事なら、**新しく作業を始めない**。
  どこで押すかを案内する。

### ★有璽氏の決定（2026-09-05 12:52）── 運用で回避せず、構造を直す

**「現場の構造は改修したいね。それは困るわね」**（＝Slackで返しても解けないのは困る）。
選択肢2（通知に機・セッションを書かせる）を採ったうえで、**その先まで直す**という指示。

```
いま        Slackは通知だけ → 押すのは端末 → ★どの端末かも書いていない
第1段       通知文に「どの機・どのセッション・何を聞かれているか」を入れる（小さい・可逆）
第2段       ★Slackから承認そのものを返せるようにする（大きい・設計の分岐）
```

- **★「Slackで押せません」を運用ルールとして配って終わりにしない。**
  有璽氏は持ち運びでMacBookを閉じる。**端末でしか押せない設計は、その前提と噛み合わない。**
  → [[reference_offload_long_work_to_mini]]（同じ理由で長い処理はminiへ寄せた）
- 第2段は**設計の大きな分岐＝承認が要る**。ピタゴラスに**実装でなく可否の調査**から出す
  （フックが承認の応答を代行できるのか、できるとして何が危険か）。
- **★第1段だけ入れて「直った」と言わない。** 直ったと言える事実は
  「有璽氏がMacBookを閉じたまま承認を返せた」。部品の言葉で判定しない
  → [[reference_verify_outcome_not_mechanism]]

---

## ★★4回目の指摘 ── 今度は「1通」でなく「全体がそろっていない」（2026-09-05 有璽氏）

> 「**クリックするだけのタイプであれば、私でも確認は取れます。今はSlack側への
> 通知方法がバラバラです。なので、それはクリックして答えれるような状況にしてください。**
> それを**AI、Vivi側が徹底してくれれば、こちらは答えやすいので答えます。**」

**★1〜3回目は「1つの通知が記述式に戻った」。4回目は範囲が違う ──「全部を数えて揃えろ」。**
1通ずつ直すのをやめ、**Slackへ出る経路を全件洗った**（結果は
`~/.vivid-relay/notify_unify_result.md` の表）。

### ★4回目になった理由は「直していなかったから」ではない。3回直して3回消えたから

```
真因（2026-09-05 実測2経路）
  bin/vivid-sync.sh:194 が */15 で bin/setup_hooks.sh を★無条件に呼ぶ
  setup_hooks.sh:38-47 が bin/hooks/*.py を ~/.vivid-relay/ へ★上書きコピーする
  ＝ ~/.vivid-relay/notify.py だけを直すと、15分以内に bin/hooks/ の旧版で消える
★9/4の「ビビが2体走っていた」は誤診。同じ日に直した bin/hooks/self_audit.py は生きている
```

**→ `notify.py` の正本を `bin/hooks/` 側へ移した。** 詳細と見分け方は
[[reference_fix_where_git_reaches]]（★同じ型は 2026-08-27 に既に書かれていて、読まれなかった）。

### いまの経路（2026-09-05 夜・実測）

```
◎押せる   ask_hub.ask() ／ notify.ask()（★ask_hub へ委譲し直した）／
          findings_escalate（08:15）／ stall_watch（未登録）／ intake_notify（営業ch）／
          GAS議事録botの承認
✕押せない  hook_permission_slack（★構造的に返せない・別セッションが改修中）／
          self_audit・memory_sweep の tell()（AIの自由出力・いまは stderr 警告のみ）／
          notion_minutes_duplicate_report・automation_inventory_check（#09チャンネル宛）
```

### ★徹底の担保は「新しいフック」ではなく「既に走っている検査」へ足した

**★フックを足しても settings.json への登録が人の手待ちで、登録されるまで0本と同じ。**
実例 ── 層2の `hook_interactive_guard.py` は 9/5 に実装・実測まで済んで**いまも未登録**。

```
入れたもの  bin/hooks/self_audit.py の _buttonless_dm_check()（毎朝08:40・登録済みの経路）
判定        有璽氏のDMのIDを持つ ＋ chat.postMessage を叩く ＋ ★blocks（ボタン）が無い
            ＝この3つのANDに当たる .py が、既知の3本より増えたら人の目に入れる
できないこと GAS・シェルは見ていない／tell()経由の判断語は見ていない（実行時のstderr担当）
```

- **★「ボタンは blocks でしか出せない」を代理指標にした。**文字列の有無で見るだけなので
  完全ではないが、**登録の人手を1つも増やさずに毎朝効く**方を採った。
- **★壊して1回確かめた**（該当する .py を一時ディレクトリに置くと鳴る）。
  正常しか出せない検査になっていないことを実測した → [[reference_a_warning_nobody_owns]]
- **★通し検証はまだ**。本物のボタンを押す確認は、`findings_escalate` に対象が出た朝まで持ち越し。
## 🔴🔴 ★3回目 ── ターミナルに表で並べるのも「選択式」ではない（2026-09-05 有璽氏）

> 「**フラッグはプッシュ通知で押せる状態にしてくれたら、そっちの方**、っていうか
> **それをマストにしてほしい**ので、はい、いえというか**選択肢プラスそれ以外のものは記述する**
> みたいな、そういう形の質問回答の形にしてほしい。」

**★ビビが判断4点をMacBookのターミナルへ表で出し、「推奨で、の一言で通せます」と書いた回。**
選択肢は示していた。**それでも不可だった。**

```
✕ ターミナルに選択肢の表を書く       ★見るには端末の前に居る必要がある
✕ AskUserQuestion（端末のダイアログ）  ★don't ask mode で拒否される・端末依存も同じ
◎ ask_hub.ask()                      ★プッシュ通知で届く・押せる・その他は記述で返せる
```

- **★「選択式」の要件は3つそろって初めて満たされる** ──
  ①**プッシュで届く**（端末の前に居なくてよい）②**押せる**（打たない）
  ③**それ以外は書いて返せる**（選択肢に無い答えを潰さない）。
  **①が抜けているものは、選択肢が並んでいても不可。**
- **★これまで2回の指摘は「記述式に戻った」だった。今回は「端末に出した」。**
  形は違うが、有璽氏から見れば同じ ──「**判断を求められたのに、その場で押せない**」。
- **★ビビ自身が最も外しやすい。** 担当は Slack へ出すが、
  **窓口は有璽氏が目の前に居るので、つい会話の中で聞く。**
  ★**目の前に居ても、判断は ask_hub へ出す。**会話で補足するのは構わないが、
  **押せる本体を必ずSlackに置く。**
- ★ask_hub の「✍️ その他（書いて返す）」が要件③に当たる。**必ず付ける**（既定で付く）。

## 🔴🔴🔴 ★詰まりの真因は「経路がバラバラ」だった（2026-09-05 有璽氏・★これが答え）

**ビビが「クッションが詰まる。①何日で ②誰に ③どう上げ直すかを決めてほしい」と出したのに対し、
有璽氏の答えは「①②③を決める」ではなかった。**

> 「この点に関しては、**クリックするだけのタイプであれば、私でも確認は取れます。**
> **今はSlack側への通知方法がバラバラです。なので、それはクリックして答えれるような状況に
> してください。** それを**AI、Vivi側が徹底してくれれば、こちらは答えやすいので答えます。**」

```
ビビの見立て   詰まるのは「詰まりを誰も拾わない」から → 期限と宛先と再送を設計しよう
★実際          ★詰まるのは「押せないものが混ざっている」から
                クリックだけで済むなら、有璽氏は詰まらせない（実測：今日の4件は★1〜3分で回答）
```

- **★②「誰に」の答えが出た ── 有璽氏でよい。ただしクリックだけで済む形なら。**
  宛先を分散させる設計は要らなかった。**形式を揃えるほうが先。**
- **★「徹底」がこちらの仕事。** 有璽氏は答える側の準備ができている。
  **経路がバラバラなまま期限や再送を足しても、押せないものは押せないまま残る。**
- **★これは4回目の同種の指摘**（9/4×2・9/5 端末の表・今回）。
  1〜3回目は「1つの通知が記述式に戻った」。**今回は「全体がそろっていない」＝範囲が違う。**
  → **1通ずつ直すのではなく、Slackへ出る経路を全部数えて揃えるところまでが今回の依頼。**

### ★揃えるべき経路（実測して数えること。この一覧を鵜呑みにしない）

```
判断を求めるもの   必ず ask_hub.ask()（ボタン＋その他記述）
  notify.ask()          ★9/5 層3で ask_hub へ委譲済み（notify.py に ask_hub 30件を実測）
  findings_escalate.py  notify.ask() 経由 ＝ 委譲が生きていれば自動でボタンになる
  self_audit / memory_sweep  判断語を検出したら ask_hub へ回す実装（★巻き戻り履歴あり・要実測）
  🔴 hook_permission_slack.py  ★構造的にSlackで押せない。通知するだけで承認は端末
                               → 有璽氏の決定は「運用で回避せず構造を直す」（同ファイル上部）
報告               notify.tell()。冒頭に「返信は要りません」。判断語を1つも入れない
営業の受付確認      intake_notify.py ＝ ボタンあり（既存・#01_営業部門-ai確認依頼）
```
